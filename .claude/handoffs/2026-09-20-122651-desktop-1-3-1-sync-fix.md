# Handoff: Desktop 1.3.1 Sync Fix

## Session Metadata

- Created: 2026-09-20 12:26 (Asia/Shanghai)
- Project: D:/Develop/EggDone
- Branch: main
- Code commit: b52fc57 (`fix(sync): stabilize desktop auto sync for 1.3.1`)
- Scope: desktop sync fixes, version bump, local build and handoff. No push or publication.

## Handoff Chain

Continues from [Desktop 1.3.0 lifecycle release](2026-09-19-201946-desktop-1-3-0-lifecycle-release.md). Supersedes its current desktop version and next-action guidance, not historical acceptance evidence.

## Current State Summary

Desktop is 1.3.1 in package.json, Cargo.toml, the eggdone entry in Cargo.lock and tauri.conf.json. The user reported prolonged syncing/pending/syncing after creating a task, even though Harmony already received it, and no auto sync when revealing the desktop window. Scheduling and native-show fixes are committed. Windows release executable was rebuilt and FileVersion/ProductVersion both verified as 1.3.1. Actual user-cloud timing and tray acceptance remain unconfirmed. Harmony stays unchanged at 1.4.0 / 1000031.

## Important Context

- User authorized desktop version bump, handoff and local commits only. Do not push, publish, change Harmony or begin calendar work without another request.
- User previously reported desktop 1.3.0 and Harmony 1.4.0 packaged and published. This does not imply 1.3.1 publication or acceptance.
- Database schema 26, backup v8, sync protocol and storage locations are unchanged. Preserve automatic trusted discovery; do not reintroduce manual space selection or mandatory migration for recycle-bin deletion.
- Automated tests are not native/cloud acceptance. No user data or cloud objects were modified for diagnosis; the installed application was not force-closed.
- Task cwd is D:/Develop/EggDoneHarmony, but desktop work belongs to D:/Develop/EggDone. Verify paths and git state before commands. Harmony status was clean at preparation.

## Codebase Understanding

Svelte autoSync owns scheduling, debounce, foreground polling and status. Native show_panel emits a main-window event. Rust sync retains per-domain successful receipts, target guards and dirty snapshots. Coalescing redundant triggers must never discard real mutations during a running sync.

### Critical Files / Files Modified

| File (desktop-relative) | Change |
| --- | --- |
| src/lib/sync/autoSync.ts | Coalesce running requests, consume debounce, preserve mutation-driven follow-up, foreground version and syncOnPanelShown. |
| src/lib/sync/autoSyncWindow.ts | New listener lifecycle helper; subscribe before visibility query and reject stale results. |
| src/lib/sync/autoSync.test.ts | Seven new scheduling regression cases. |
| src/lib/sync/autoSyncWindow.test.ts | Six native-event and lifecycle tests. |
| src/lib/components/TodoPanel.svelte | Use show/focus helper instead of focus-only startup. |
| src-tauri/src/tray.rs | Emit panel-shown to main after successful show. |
| src-tauri/src/s3_sync.rs | Extract guarded three-entity ETag check; full periodic remote check remains. |
| src-tauri/src/commands.rs | Completion probes three entities, avoiding seven discarded serial requests. |
| src-tauri/src/sync_core_tests.rs | Update completion network fixture and assertions. |
| package.json; src-tauri/Cargo.toml; src-tauri/Cargo.lock; src-tauri/tauri.conf.json | Desktop 1.3.1; dependency versions unchanged. |
| CHANGELOG.md; README.md | Release behavior, version, evidence and handoff link. |

## Work Completed

- [x] Removed false follow-up syncs from polling, duplicate foreground events and joining manual requests.
- [x] Preserved real local edits during sync and consumed obsolete debounce timers.
- [x] Fixed quick hide/show checks and native-show activation independent of focus callbacks.
- [x] Removed seven unused completion requests without bypassing target or dirty-state validation.
- [x] Reviewed staged changes and committed b52fc57; bumped desktop and rebuilt Windows executable.

### Decisions Made

| Decision | Rationale |
| --- | --- |
| Successful native show starts sync directly | No dependence on focus callback or full remote preflight; disabled auto sync stays disabled. |
| Only real edits queue another run | A repeated trigger is not a mutation. |
| Retain full periodic remote polling | Other devices can change any domain; only unused completion work is removed. |
| No schema/protocol migration | This is a scheduling/performance patch. |

## Validation

Before version-only changes, this sync implementation passed pnpm test (499 tests/59 files), cargo test --lib (481 passed, 40 external-environment tests ignored), cargo test sync_core_tests --lib (11 passed, 27 ignored), targeted scheduler/window/status tests (62 passed), and pnpm i18n:check (1044 aligned keys and hardcode check).

After changing metadata to 1.3.1:

- pnpm test repeated: 499 passed / 59 files.
- pnpm check: zero errors/warnings.
- cargo check --locked and cargo fmt -- --check: passed.
- pnpm tauri build --no-bundle: frontend production and Windows release passed.
- Artifact: D:/Develop/EggDone/src-tauri/target/release/eggdone.exe. Inspected FileVersion and ProductVersion: 1.3.1.
- Staged git diff whitespace check passed before code commit.
- Existing warnings: frontend chunk above 500 kB and seven Rust unused-code/field warnings; no unrelated cleanup.
- Not done: 1.3.1 installers/Linux packages, installed-app replacement, actual native tray/cloud timing, Linux runtime acceptance. Existing 1.3.0 Linux packages are not new-version artifacts.

## Immediate Next Steps

1. User exits installed desktop through its tray menu, then runs the 1.3.1 executable above. Do not force-close it or start another instance as a substitute for acceptance.
2. With auto sync enabled, hide/reveal via tray and verify sync starts. Create a task, verify Harmony receives it and desktop settles without another pending/syncing cycle when no new edits occurred.
3. Edit during sync and verify that edit reaches Harmony. Test quick hide/show and disabled auto sync. If delay persists, gather sanitized stage timings/errors rather than assume all latency is explained.
4. Record manual acceptance before separately requested packaging or feature work.

## Pending Work

No known build blocker. Actual native/cloud behavior remains an open acceptance question; no user-cloud timing was collected.

### Deferred Calendar Feature

User explored read-only Harmony system-calendar event display and syncing that representation to desktop. No reminders, calendar writes or automatic task conversion. User said ACL was applied for; approval, updated Profile and device/API suitability are unconfirmed. No calendar implementation, permission declaration, signing change or event access was performed. Before separately authorized implementation, verify official READ_WHOLE_CALENDAR/API requirements, appropriate ACL-bearing signing Profile and runtime user grant. Old Profiles do not automatically acquire newly approved permissions.

## Environment State

- Windows/PowerShell, pnpm, Rust, Tauri 2/Svelte. All build/test commands for this work completed; no new app or server was launched.
- Earlier installed desktop instance was left running. Reinspect before replacing or launching; its PID is not an acceptance signal.
- No credential values are recorded. PYTHONUTF8 may be needed for Chinese handoff tooling output on Windows.
- Build outputs remain ignored. Do not use actual cloud deletion or overwrite user databases for regression tests.
- Preserve isolated browser-test/development Vite caches; prior shared-cache Svelte black-window issue is historical context, not a reason to alter data.

## Related Resources

- [Release notes](../../CHANGELOG.md)
- [README](../../README.md)
- [Lifecycle regression](../../docs/TASK_LIFECYCLE_REGRESSION.md)
- [Previous handoff](2026-09-19-201946-desktop-1-3-0-lifecycle-release.md)
- Validator: C:/Users/caozhipeng/.agents/skills/session-handoff/scripts/validate_handoff.py
