# Handoff: 桌面 1.1.0 升版与 main 合并

## Session Metadata

- 日期：2026-09-13；仓库：D:/Develop/EggDone。
- 当前分支 main，来源 codex/experience-roadmap 保留。
- 升版前 841e8eb，原 main fa2c052，升版提交 ced5908，合并提交 6d728ab。
- 配套鸿蒙 D:/Develop/EggDoneHarmony，1.2.0 / 1000029，版本提交 a46c125，合并 de7227a。
- 前序交接：本目录 2026-09-12-001404-desktop-e6b3-system-capabilities.md；本篇覆盖当前状态，保留历史验收边界。

## Current State Summary

按用户要求将桌面从 1.0.10 升至 1.1.0，前端包、Cargo 包和锁文件、Tauri 配置保持一致。更新 changelog、README、双端升级指南和 roadmap，非快进合并整个体验优化分支到本地 main，无冲突且合并树与已验证来源分支相同。本轮仅改版本与文档，没有新业务迁移或依赖升级；未生成正式安装包、未推送或发布。本文校验后单独提交。

## Architecture Overview

Svelte/TypeScript 前端、Tauri 2/Rust 后端与 SQLite schema 19。components 展示、stores 编排、api 封装原生命令，Rust 模块承担事务、同步、托盘和窗口能力。轻量、本地优先，不引入自建服务器，与鸿蒙共享 UUID、同步对象和备份契约，不共享数据库文件。

## Critical Files

| 文件 | 用途 |
| --- | --- |
| package.json | 前端包版本与检查入口 |
| src-tauri/Cargo.toml | Rust 包版本 |
| src-tauri/Cargo.lock | 本包锁定版本；未改第三方版本 |
| src-tauri/tauri.conf.json | 应用版本与 com.eggdone.desktop 标识 |
| CHANGELOG.md | 1.1.0 变化及边界 |
| docs/EXPERIENCE_UPGRADE_GUIDE.md | 双端升级、混用与备份说明 |
| docs/PREFERENCES_NATIVE_RUNTIME.md | 尚未通过的隔离原生重启工具 |
| docs/CONTENT_SEARCH_NATIVE_RUNTIME.md | 已有原生搜索证据 |

## Work Completed

- 版本四处同步为 1.1.0。
- 原未发布汇总归入 1.1.0：任务便签关联、回收站、本地文字历史、统一搜索、固定筛选、本机偏好和系统能力状态、保存保护、按钮和下拉主题优化。
- 两端一致的升级指南明确配套版本和验证边界，roadmap 保留未完成验收，完成版本提交与 main 合并。
- 原开发分支完整纳入 main，未丢弃历史提交，不只合并本轮九个版本资料文件。

## Files Modified

版本提交包含 package.json、Cargo.toml、Cargo.lock、tauri.conf.json、CHANGELOG、README、升级指南及两份 roadmap。本文及 README 交接入口另行提交。未提交 build、target、node_modules、数据库、签名或临时截图。

## Validation

- 升版后国际化 724 键、三时区智能列表 30/29/30 组、快捷收集 12 组通过，Svelte 检查 0 错误/0 警告。
- 最初 PATH 中的 pnpm 尝试自动整理依赖时被拒绝访问；改用现有 bundled fallback 入口后依赖按已有锁文件恢复，pnpm-lock.yaml 没有变更，没有手动删除目录。
- pnpm release:check 的默认并行测试未通过：desktopSettings 首例 5000ms 超时，紧邻用例断言也失败，304 通过/2 失败。不能把这次一键检查记为成功。
- 随后 pnpm exec vitest run --maxWorkers=1 全量重跑：35 文件、306 测试全部通过，未改断言、超时阈值或业务源码。并行超时与状态残留是待观察的测试稳定性风险，不宣称已修复。
- 单独继续 pnpm build、cargo fmt -- --check、cargo check --locked、cargo test --locked，均通过。Rust 266 通过、11 项外部 S3 测试按原配置忽略；main/doc 无测试，已有 TraySnapshot.locale 未使用警告保留。
- 版本四处一致、配套鸿蒙版本/code/buildVersion、两份升级指南一致性、git diff --check 通过。
- 合并后 git diff codex/experience-roadmap HEAD 无差异，证明合并未更改已验证源码。后续仅增加交接文档和 README 链接。
- 没有构建 Windows NSIS、Linux DEB/AppImage 或 macOS 包；已有旧版本包不能当作 1.1.0 分发，本次未启动普通客户端或修改用户库。

## Environment State

Windows / PowerShell，Node/pnpm、Rust/MSVC。可用 pnpm 入口为 C:/Users/caozhipeng/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/pnpm.cmd；重跑时将该目录置于 PATH 首位，后续先检查本机工具路径。所有本轮验证进程已结束，没有启动开发服务器或设备采集。handoff 工具使用 PYTHONUTF8 环境变量，不记录任何凭据。

## Important Context

这次请求仅授权两端升版、生成 handoff、合并 main 并本地提交；不包含推送、上架、删除数据或继续 E9 开发。原“完成剩余所有开发任务”目标因完整原生/真机/用户旧库条件不足已标记阻塞，不代表所有开发计划验收通过。不要把本次版本号提升或文档校验当作解除这些门槛。

两端保持无自建服务端、本地优先与既有 S3 协议。任务便签关联独立同步，解绑不删除内容；重复任务只关联当前实例。备份数据格式 v3，可导入有效 v1/v2，旧端不能导入 v3；数据库不能直接降级给旧二进制。文字历史仅本机正文，最多每便签 100、全库 1000 条，不随 S3 或应用导出备份传输。回收站恢复不重启旧提醒/重复、不自动重连旧关联；附件元数据不保证原件仍可用。

## Immediate Next Steps

1. 先检查本仓库与配套仓库的 main、版本文件、最新提交和工作区，不依赖旧 handoff 的路径或进程号。
2. 若用户继续发布，先核对最终包来源、版本、应用标识、签名与 SHA256，并单独确认推送/上传授权；本次没有正式发布包验收。
3. 若继续验收，先取得专用 QA 内容创建/清理许可，准备真机、可恢复的用户旧库及隔离测试环境，再做原生重启、提醒、快捷键、关联/回收站/历史/搜索完整矩阵。
4. 若用户希望新增功能，另行冻结 E9 模板与单层检查清单、专注历史或自动备份的范围，不能自动启动。小艺后续开发仍暂停，桌面唤醒失败不宣称已解决；已审核实况窗核心不改。

## Pending Work

- E6：真实退出重启、快捷键冲突/恢复、系统通知权限及多窗口完整验收。
- L3d3/L4d2/L5：用户旧库、真实云端、原生附件恢复、完整设备和异常矩阵、正式包发布验收。
- E8：回收站、文字历史、统一搜索的完整原生交互、软键盘/字号/窗口组合及真机验收。
- 桌面原生隔离重启工具已提交，但此前没有跑通；测试报告解析器通过不等于原生重启通过。
- 上轮只连接手机和平板模拟器；本轮没有操作设备或修改用户数据。创建同步 QA 内容的许可仍未收到，不把自动续跑视作授权。

## Decisions Made

| 决策 | 原因 |
| --- | --- |
| 先提交版本与资料，再保留合并提交，最后单独提交 handoff | 让版本维护、分支整合和交接证据可独立追踪 |
| 不改 schema、依赖约束、签名或平台协议 | 本次是版本与分支收口，不新增业务迁移 |
| 保留 roadmap 未完成勾选项 | 部分自动化及用户局部成功不能代表完整矩阵 |
| 不推送、不上传、不打正式发布包 | 用户仅授权本地合并和提交 |

## Assumptions Made

按用户指定的桌面 1.1.0、鸿蒙 1.2.0 作为本地候选版本，版本存在不代表已经分发。历史 README 中的“未提交/未升版/旧候选”是历史记录，当前状态以 README 首段、本 handoff 和 Git 为准。

## Potential Gotchas

不要卸载应用、清理用户数据库或直接降低 schema 来做升级测试。不要覆盖正在使用的普通客户端；原生隔离启动曾被工具策略拒绝，不绕过拒绝。现有测试工具与浏览器替身结果有各自边界，既有用户验收不能转移到新增阶段。签名配置、云凭据、用户内容不能写入 handoff 或提交。

## Related Resources

- 总计划：`docs/EXPERIENCE_EVOLUTION_ROADMAP.md`。
- 关联计划：`docs/TASK_NOTE_LINK_ROADMAP.md`。
- 兼容指南：`docs/EXPERIENCE_UPGRADE_GUIDE.md`。
- 原生关联：`docs/TASK_NOTE_LINK_NATIVE_RUNTIME.md`。
- 回收站与历史：`docs/TRASH_SYNC_REGRESSION.md`、`docs/NOTE_HISTORY_SYNC_REGRESSION.md`。
