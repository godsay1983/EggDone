# EggDone 桌面端更新日志

按版本倒序记录功能、修复与版本升级。根据本地 Git、发布说明及验收记录回溯；版本存在不代表已在商店发布。

## 维护约定

- 日期为升版提交日期，当前工作区版本使用修改日期，不冒充发布日期。
- 主要汇总上次升版之后至本次升版提交内的变化。同版本升版后的补充单独注明；初始开发期的累积改动归入下一次升版。
- 后续每次升版，同步更新本文件、`package.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 和 README。关于页从包元数据读取版本。
- 新条目记录主要功能、修复、兼容性及验证边界；仅改版本号也要记录。计划不写成已实现，构建不写成正式验收。
- 历史提交号可用 `git show <提交号>` 查看；范围可用 `git log <旧提交>..<新提交>` 查看。新条目无需为了填写自身提交号反复修改。

## 1.0.10 - 2026-09-06

- 主窗口解除固定宽度限制，支持拖动边缘和四角调整大小，保留托盘显隐与独立专注窗口行为。
- 设置新增“外观与窗口”：小巧、舒适、大窗口预设，100%、115%、125%、150% 内容缩放和恢复默认；支持 Ctrl/Cmd + 加号、减号、0。
- 本机记忆逻辑尺寸和缩放比例，按屏幕工作区及 DPI 约束大小和位置；补充快速隐藏/显示后的尺寸保存、设置失败回退和事件清理。
- 调整设置面板宽度及主面板标题栏换行，避免窄窗口控件挤压。偏好不参与 S3 同步，不改变数据库、重复规则或依赖版本。
- 升版同步包元数据、Rust 包、锁文件、Tauri 配置与 README；关于页随包版本更新。
- 自动验证：155 项单元测试、16 组中英文/深浅主题/宽度浏览器组合及预设、缩放、快捷键、恢复、DPI、错误回退等断言通过；Svelte、国际化、前端构建、Rust 检查通过，保留已有警告。
- 浏览器使用原生接口替身；实际窗口拖动、WebView 缩放、托盘、多显示器及 Linux/macOS 验收待人工确认。此前生成的 Linux 1.0.9 安装包不包含本版改动；本版尚未发布。

## 1.0.9 - 2026-09-06

- 版本升级：统一桌面包、Rust 包、锁文件与 Tauri 配置的版本，关于页随之更新。
- 修复日程面板“自定义重复”按钮使用浏览器默认样式的问题，复用同面板按钮的圆角、字号和亮暗主题配色，补充键盘焦点与中英文窄屏回归。
- 更新 README 当前版本和用户验收状态；新增本更新日志，补齐历史升级说明。
- 没有新增业务功能，不改变数据库、同步协议或依赖版本。
- 验证：Svelte 检查、前端构建、Rust 格式检查、`cargo check --locked` 和版本一致性检查通过；已有 `TraySnapshot.locale` 未读警告保留。
- 按钮修复回归：21 个真实 Svelte 浏览器场景通过，含新增中英文/亮暗主题入口样式、悬停、焦点及打开操作；已查看截图。IPC 使用替身，不替代原生系统验收；浏览器阻止 Vite 热更新 WebSocket，未影响本次页面加载及断言。
- 来源：1.0.9 版本维护与按钮修复提交。未重新执行完整功能验收，未发布本版本；上一轮用户验收记录为 `1fb6878`。

## 1.0.8 - 2026-09-06

- 新增智能列表：逾期、未来 7 天、无日期、重要、最近完成，可配合分组和搜索。
- 新增快捷键、命令行快速收集；按平台能力支持从任务通知完成任务。
- 新增自定义重复编辑器：日/周/月间隔、星期、月内日期或末日、次数/日期结束、全天或固定时区时间；支持摘要、修改、停止与历史保护。
- 重复规则通过独立 S3 对象同步；完善事务、幂等下一实例生成、条件上传、配置切换与迟到回包隔离。
- 规则及实例回执纳入 v2 备份与原子恢复，持久化同步诊断与待同步状态。本阶段桌面数据库为 v17。
- 同版本补充：真实 S3 三段传输集成测试、双端回归资料和用户验收记录，见 [NS7 记录](docs/NS7_REGRESSION_AND_RELEASE.md)。
- 任务便签关联、专注历史和加密同步仅完成后续设计，不是已开放功能。
- 来源：`5e13c61..3c094a7`；后续补充 `a4e27f6`、`1fb6878`。

## 1.0.7 - 2026-07-25

- Linux 托盘改用 ksni 支持左键激活面板，保留平台回退路径。
- 优化任务摘要、菜单按钮和待办计数文案，改善英文布局。
- 来源：`c5dbf04..5e13c61`，托盘改动 `7690d0d`。

## 1.0.6 - 2026-07-18

- 新增简体中文、English、跟随系统语言模式及运行时切换。
- 本地化任务、便签、附件、设置、同步、数据管理、专注、托盘和通知。
- 快捷新增支持双语语法；错误采用稳定错误码并显示对应语言的说明。
- 增加翻译键、占位符、硬编码和共享语法检查；关于页改为读取包版本。
- 来源：`3788c46..c5dbf04`，详见 [国际化发布说明](docs/INTERNATIONALIZATION_RELEASE_NOTES.md)。

## 1.0.5 - 2026-07-18

- 改善自动同步错误处理、重试及状态提示，降低短暂网络失败对主界面的干扰。
- 补充同步异常回归测试与交接资料。
- 来源：`cf0bbea..3788c46`，核心修复 `b583496`。

## 1.0.4 - 2026-07-16

- 便签支持图片和普通文件附件、本地资源存储、缩略图、S3 二进制及元数据同步。
- 支持按需下载、缓存统计与清理、远端附件清理，以及删除撤销。
- 新增附件管理器；图片与文件按类型分组排序，优化便签摘要、图片查看器和操作配色。
- 支持附件元数据导入导出、含二进制的完整备份导出及原子恢复。
- 来源：`6595fd7..cf0bbea`。

## 1.0.3 - 2026-07-14

- 统一便签编辑器按钮的亮暗主题配色及危险操作样式，改善暗色主题可读性。
- 来源：`6595fd7`。

## 1.0.2 - 2026-07-13

- 新增文字便签、编辑与自动保存，统一空白草稿退出时不创建便签的行为。
- 支持便签 S3 同步、导入导出，在设置展示自动推导的便签 Object Key。
- 增加便签存储、同步和人工回归资料。
- 来源：`bc78b0b..3234472`。

## 1.0.1 - 2026-07-04

- 任务下方展示所属分组标签，与鸿蒙端的分组信息展示保持一致。
- 来源：`bc78b0b`。

## 1.0.0 - 2026-06-18

- 新增重要标记、四象限、日历和日程分桶；快捷新增识别重要标记。
- 新增番茄钟、独立专注窗口、可配置时长、阶段通知、关联任务及完成任务操作。
- 提供专注/休息/完成素材、休息配色、托盘状态与专注控制，以及展开/胶囊形态切换。
- 将应用版本统一为 1.0.0。
- 来源：`a25a932..92ca05e`。其中 `df799b5` 仅将部分元数据对齐为 0.1.1，不另算新版本。

## 0.1.1 - 2026-06-15

- 完善编辑、排序、置顶、搜索、备注、分组、批量操作、归档和删除撤销。
- 新增到期日期、分钟级时间、提醒与稍后提醒、快捷新增语法、基础重复任务及系列操作。
- 接入 S3/MinIO、ETag 冲突保护、自动同步与前台探测，以及 JSON 导入导出和本地备份。
- 完善全局快捷键、开机启动、多显示器定位、托盘计数和 Windows 通知动作。
- 来源：`a9a4abc..a25a932`，汇总初始版本之后逐步积累到本次升版的功能。

## 0.1.0 - 2026-06-06

- 初始化 Tauri、Svelte、Rust 桌面工程，建立托盘 Todo 及本地开发基线。
- 来源：`a9a4abc`。初始版本期间后续功能统一汇总到 0.1.1。
