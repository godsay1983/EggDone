# 关联功能原生运行验证（L4d2 分批记录）

日期：2026-09-12。第一批产品源码基线：桌面 d153b95，鸿蒙 b549e42。第一批新增测试与文档，不改产品代码。第二批桌面UI异常出口修复见下文；均不改数据库结构、权限、版本号或小艺能力。

## 第一批结论与边界

本批完成 Windows 原生 WebView2/SQLite 基础关联与附件往返，以及鸿蒙手机/平板模拟器真实 Markdown 导入、关联创建、原文件下载。L4d2完整矩阵、L3d3真实用户环境和L5发布门槛仍未完成。

这不是桌面与鸿蒙之间的真实云同步验收：桌面运行在无同步配置的隔离测试实例；本次观察到的云端传递仅发生于两个沿用原同步配置的鸿蒙模拟器。

## 桌面原生回归

新增 `scripts/test-link-native-webview.mjs`（仅在桌面仓库）。连接本机9228端口的原生主WebView，首先调用实际Tauri接口确认标识为 `com.eggdone.l4d2test`，不匹配即拒绝写入与截图。无IPC mock，不修改生产权限。

最终运行报告含8组通过项：

1. UI输入标题与100行正文，通过实际Rust命令保存；经HTML文件输入导入Markdown与仓库128x128 PNG，原文件逐字节读回一致。
2. 图片实际解码宽度128；Escape先关闭图片，再关闭附件管理，原便签保留。
3. 从便签创建关联任务，打开任务、返回来源；正文滚动位置恢复，附件仍为2项，没有复制。
4. 管理关联中选择解除后取消，实际存储关系保持。
5. 原生100%缩放，创建面板提交按钮可达，取消不新增关系。
6. 原生125%缩放，同上。
7. 原生150%缩放，同上。
8. 重新加载WebView后，便签、附件和关系从实际存储恢复。

窗口通过Tauri的真实窗口尺寸和WebView缩放接口调整，各档目标逻辑尺寸为360x560、450x700、540x840；检查CDP报告的实际缩放和按钮视口边界。不是CSS zoom，不代表操作系统DPI、多显示器或所有最小窗口组合。

截图采用CDP整张surface，避免Playwright默认CSS clip在原生缩放下将右侧与底部裁掉。已检查整张150%截图，取消和创建任务均完整可见。文件输入通过Playwright提供真实文件字节，不宣称测试了Windows系统文件选择对话框；PNG为应用原创图标测试素材，不替代多种用户图片质量验收。

### 复现

在桌面仓库、没有该隔离测试实例运行且本机9228端口空闲时执行：

```powershell
node node_modules/@tauri-apps/cli/tauri.js build --debug --no-bundle --config '{"identifier":"com.eggdone.l4d2test"}'
$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS='--remote-debugging-port=9228'
$testProcess = Start-Process ./src-tauri/target/debug/eggdone.exe -WindowStyle Hidden -PassThru
node scripts/test-link-native-webview.mjs
```

脚本使用Playwright；如未安装在项目中，用 `PLAYWRIGHT_PATH` 指向现有Playwright模块目录。测试要求应用为中文界面，不修改用户语言。运行者应等待CDP端口就绪，结束后停止自己启动的测试进程并清除当前shell的调试环境变量，不暴露调试端口。

测试数据只在 `%APPDATA%/com.eggdone.l4d2test` 中，保留QA记录供复核；脚本每次生成新标题，失败也写report.json。它不会安装客户端，拒绝对正式应用标识继续测试。此构建覆盖本地debug exe的应用标识，不是发布包，正常使用应重新按默认配置构建。

最终报告目录：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-link-native-1789192423906`。本轮测试进程19248已停止。此前调试选择器失败和截图裁剪问题是测试脚本问题，修正后重新完整运行通过。

## 鸿蒙模拟器

设备实际核对：手机Mate 80 Pro Max `127.0.0.1:5557`，平板MatePad Pro 13 `127.0.0.1:5555`。均为模拟器，没有连接物理设备；沿用上一轮已覆盖安装的b549e42 Debug包。本轮未重新构建/安装鸿蒙包、未运行ohosTest。

- 手机新建临时便签 `QA-L4D2-0912-1342`。系统文件选择器“最近”为空，改为“浏览 → 我的手机 → Download”，选择既有64 B测试文件 `E5A-QA-attachment.md`，成功导入。
- 附件管理显示“64 B · 本机可用”。系统返回关闭附件管理，保留同一编辑器与附件。
- 从添加菜单创建同名关联任务，面板确认后显示“关联任务（1）”；展开后打开任务详情，系统返回回到同一便签，附件仍为1项。
- 平板收到该便签、关联计数和文件元数据。管理面板最初为“原文件待下载”；点击保存弹出系统保存选择器，取消导出后变为“64 B · 本机可用”。这证明下载完成和系统选择器调用，不证明用户目录中的导出文件写入。
- 本轮未导入鸿蒙图库图片；不能以桌面PNG验证替代鸿蒙相册/图片完整往返。

控件树位于对应模拟器的 `/data/local/tmp`，截图位于Windows临时目录，前缀 `l4d2-`；主要证据为 `l4d2-manager.json`、`l4d2-returned.json`、`l4d2-tablet-asset.json`、`l4d2-tablet-downloaded.json`、`l4d2-tablet-save.png`。

### 数据清理

仅删除本轮新建的QA便签及其关联任务，走应用正常删除/墓碑流程。手机任务列表恢复为原有3项，便签恢复为原有test；平板重开后列表同样只剩原有test。未删除历史QA任务、用户任务、原始文件或既有便签。测试记录曾按原设置同步，删除墓碑也可能继续传播；不手动清理云端对象或墓碑。

## 本轮门槛

- 桌面原生debug build（含前端build）通过；svelte-check为0错误0警告；29文件281项单元测试通过；Rust fmt/check通过，保留既有tray locale未使用告警。
- 8组原生脚本通过。鸿蒙上述操作为系统选择器、真实应用与真实RDB，不是宿主替身。
- 没有新的产品代码修复，不推送、不升版、不发布。

## 第二批：原生写入失败与关联冲突

2026-09-12补充，开始时桌面23c649a、鸿蒙7c0e5fd。新增桌面 `scripts/test-link-native-failures.mjs`，使用与第一批相同的隔离应用标识和本机9228端口；先校验标识，再打开隔离库。需要支持 `node:sqlite` 的Node，本机Node24验证通过。

### 真实故障及修复

- 在隔离SQLite中，仅为本次生成的QA便签安装内容UPDATE拒绝触发器；不改用户库，不给正常用户路径添加故障开关。
- 输入新正文后确认数据库仍为旧正文，编辑器显示错误和重试。点击删除后正文与便签均保留，创建关联也被阻止。
- 但是删除按钮的异步回调仍将保存异常传播为未处理Promise。初轮实际WebView捕获1条pageerror，测试失败；数据保护并未失效，不能描述成误删数据。
- 桌面编辑器与便签列表统一调用 `requestNoteDeletion` 这一UI边界，消费已经由NoteStore展示的失败。底层 `deleteNote` 仍拒绝失败调用，保存/删除失败不清空选择、不丢草稿、不显示成功。
- 宿主测试保留底层拒绝断言，补充两个UI入口绑定、失败保留和成功删除断言；没有放宽原有保存要求。

### 修复后原生结果

1. SQLite真实保存失败时，删除和关联创建被阻止；原记录未删除，脏正文仍在。
2. 移除触发器后点击重试，实际SQLite正文更新为保留的输入。
3. 关联管理停留在旧版解绑确认时，通过另一原生调用解绑再重连。旧确认被实际Repository的预期版本检查拒绝；重新选择和确认后解绑成功，源便签未删除。
4. 上述流程中pageErrors为空。触发器由finally清理，核对剩余数0；脚本不移除其他运行遗留的触发器。
5. 第一批8组原生关联/附件/滚动/缩放回归重新运行通过。

失败报告：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-native-failures-1789193152250`。
修复后报告：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-native-failures-1789193315073`。
基础回归：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-link-native-1789193317794`。

重新构建隔离Debug包（含前端build）成功；svelte-check 0错误0警告；29文件281项单测、11项生产编辑生命周期、生产UI边界测试通过；Rust fmt/check通过，既有tray locale告警不变。正式发布需正常构建，不使用此隔离exe。

### 鸿蒙布局验证状态

本批没有修改鸿蒙产品代码，没有新增鸿蒙QA记录。实际device view/控件树为5555平板、5557手机，但emulator list曾给出相反的端口映射；不能仅凭该列表选择设备。

平板控制中心原有旋转锁定已开启。临时关闭后，CLI旋转命令虽返回成功，实际控件树尺寸仍为2880x1920；手机仍为1320x2848。未据此认定应用旋转适配失败，也不认定通过，继续保留原生矩阵。按原状态重新开启平板旋转锁定，旋转尝试左右成对恢复；未改字号、语言、主题或键盘设置。

键盘、大字体、分屏、小平板、原生加载失败、归档/删除端点完整矩阵、L3d3用户环境、E6人工验收和L5仍待完成。本批只收口已获得实际证据的故障，不替代这些门槛。测试实例22868已停止，隔离库QA记录保留供复核。


## 第三批：原生端点变化与读取恢复

2026-09-12，桌面源码基线0d29ece，鸿蒙037cb35。桌面新增 `scripts/test-link-native-endpoints.mjs`，沿用隔离标识、真实WebView2/SQLite和9228端口，运行前校验应用标识。此次未改产品代码，鸿蒙仅同步文档。

6组实际原生结果：

1. 已完成任务仍可从便签打开，返回后保持源便签。
2. 打开管理面板后，仅给本次QA任务写入归档状态，模拟外部状态到达；旧打开按钮重新读取并显示归档提示。关联不变，用户确认解绑后只生成关联墓碑，不取消归档或删除实体。
3. 仅将本次QA关联的record_json临时改成无效JSON。真实原生读取报错，管理面板显示失败而非空列表，不能选择候选；还原后点重试显示原关联。finally恢复本脚本的故障记录。
4. 通过原生命令删除目标任务，旧管理面板不能打开它；原生命令恢复任务后，关联墓碑不复活。
5. 选择候选任务进入确认后，用原生命令删除候选；旧确认被拒绝，没有新增关联，也没有删除源便签。
6. 从任务打开另一便签再返回；目标便签被原生命令删除后旧入口不能打开，恢复便签不重连，最终仍可返回最初来源。

最终报告：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-native-endpoints-1789194281747/report.json`。6组通过，pageErrors为空，readFaultRestored为true。

中间两次失败分别是测试误用“返回来源”定位便签内的“返回”，以及误把DOM最后一个dialog当成顶层弹窗；修正为实际可访问按钮后完整重跑通过，不记为产品缺陷。未放宽端点状态、墓碑或数据库断言。

归档和无效JSON由测试直接写入**本次生成的隔离记录**，用于验证原生读取/UI保护，不代表真实云端归档传输或普通用户自然损坏复现。删除、恢复、关联操作走实际Tauri命令。测试不操作用户库，不修改同步配置、权限或版本。QA记录保留，测试进程29124已停止。

本批补足桌面上述端点异常证据，不关闭鸿蒙对应原生矩阵、物理设备、旧库及完整发布门槛。脚本语法检查、svelte-check（0错误0警告）、前端build、Rust fmt/check通过；既有tray locale告警不变。本批未重新运行鸿蒙构建或ohosTest。

## 继续验证

- 手机/小平板横竖屏、分屏、大字体、软键盘完整原生矩阵。
- 鸿蒙真实图片、系统文件导出写入、更多文件来源和附件预览返回。
- 原生删除/归档/加载失败/解绑冲突/保存失败的完整恢复流程。
- 桌面真实进程重启、DPI/多显示器与完整语言主题组合。
- L3d3用户旧库升级、物理设备、双端真实云端同步和文件恢复；既有E6人工验收继续保留。

这些项目不能因为本批通过而勾选为完成。
