# 双端统一搜索实现方案

更新日期：2026-09-12。阶段：E8c1 双端内核已验证；E8c2-D 桌面界面已实现，E8c2-H 鸿蒙界面待接入，均未发布。

## 目标与范围

在同一入口检索任务、便签和附件文件名，按类型分组并显示命中摘要；保留当前任务/便签列表的局部快速筛选，不替换其语义。两端共享检索契约，不强求相同布局。

- 任务：当前标题与备注，包括已完成和已归档任务，不受首页分组、今天或隐藏已完成等筛选限制。
- 便签：当前标题与正文，不查询历史版本。
- 附件：只匹配显示文件名，返回所属便签及正文摘要；不匹配附件二进制、PDF 正文、OCR 或文件路径。
- 排除软删除任务、便签和附件，以及所属便签已删除或不存在的附件。
- 不自动下载/打开附件，不更改完成、归档、关联、同步状态；搜索词不持久化、不上传、不写普通日志。
- 没有数据库或同步协议迁移；使用当前已迁移的本地库。原始用户旧库与物理设备验收单列。

## 检索契约

共享数据：`docs/fixtures/content-search-v1.json`，两端文件逐字节一致。

| 项目 | 约定 |
| --- | --- |
| 输入 | 去除首尾 Unicode White_Space 与 BOM，包括 NEL；最多 100 个 Unicode 码点，拒绝 NUL |
| 匹配 | 连续字面子串；ASCII 字母不区分大小写，中文原样匹配；不承诺 Unicode 全量大小写折叠 |
| 特殊字符 | 百分号、下划线、反斜杠和引号均是普通文字，不支持 SQL 通配符或正则 |
| 空输入 | 返回空列表，不枚举全部资料 |
| 分组 | `todo`、`note`、`attachment` 分别查询；每组有独立总数和分页 |
| 页范围 | 默认 20 条，最多 50 条；offset 为 0 到 100000 的整数 |
| 排序 | 标题/文件名命中优先，再按 updated_at 降序和 UUID 升序 |
| 摘要 | 最多 160 码点；正文命中时保留其前方最多 32 码点，标题命中且正文未命中时取正文开头 |
| 附件结果 | 文件名、所属便签 UUID/标题、正文开头摘要，不返回本地路径或传输错误详情 |
| 一致性 | 每组总数与当前页在一个数据库事务内读取；不同组、不同页不是长期快照，后续修改可能改变分页 |
| 目标复核 | 打开前按类型和 UUID 重新读取最新内容及归档/完成状态；已删除或失去父便签则不可打开 |

查询必须使用绑定参数。SQL 模板按固定类型选择，不拼接输入；不用重型索引或拉取全部正文后在 UI 线程筛选。大库性能仍需实际规模验证，现有测试规模不是生产性能承诺。

错误不能伪装为零结果：无效请求使用 `SEARCH_INVALID_*`，目标失效使用 `SEARCH_UNAVAILABLE`，数据库失败统一为 `SEARCH_DATABASE_FAILED`，不向界面透出 SQL 或敏感路径。Tauri 参数解码失败可能在命令执行前返回框架错误，UI 也必须当作失败处理。

## 实现分层

- 桌面 `src-tauri/src/content_search.rs`：只读检索和目标复核。
- 桌面 `content_search_commands.rs` 和 `src/lib/api/contentSearchApi.ts`：注册 Tauri IPC 和类型化调用，不改现有列表命令。
- 鸿蒙 `models/ContentSearch.ets`、`data/repositories/ContentSearchRepository.ets`：共享返回结构和原生 RDB 事务读取，沿用现有队列。
- 桌面 `contentSearchStore.ts` 管理分组请求、分页和竞态，`ContentSearchDialog.svelte` 管理结果及归档只读预览；TodoPanel 只负责来源保存和目标导航。鸿蒙待接入独立 Store 与 ArkUI 组件；数据库查询不得搬进 TodoPanel/Index 或 UI 组件。
- 鸿蒙不能只检查 `goToFirstRow()`：原生查询失败可能返回 rowCount=-1 的结果集。总数查询须确实返回一行，分页/目标结果集须有效，分页实际读取数须与结果集行数一致；失败回滚，不返回部分成功。

## Roadmap

- [x] E8c1：冻结检索契约、双端内核/IPC、共享用例、故障与分页回归、手机/平板原生数据库验证。
- [x] E8c2-D：桌面入口、分组分页、归档只读、附件定位及保存/返回上下文；自动化证据见下文，不等于原生验收。
- [ ] E8c2-H：鸿蒙同契约的独立 Store、搜索面板及导航，手机/平板/分屏适配与自动化验证。
- [ ] E8c2：双端统一搜索入口、分组结果、摘要、独立分页、清空/空白/加载/失败/重试与中英文文案。
- [ ] E8c2：过期请求隔离、输入切换/翻页/关闭竞态；部分组失败不能显示为全部检索成功。
- [ ] E8c2：复核后打开任务、便签或定位附件；已归档任务只读查看，不静默取消归档。
- [ ] E8c2：来源便签先保存，失败不跳转；与历史/关联入口互斥，返回恢复搜索词、结果、页和滚动位置。
- [ ] E8c2：桌面窄窗口/缩放、鸿蒙手机/平板/分屏、大字体、键盘和深浅主题适配。
- [ ] E8c3：两端原生 UI 完整检索/打开/返回/编辑同步回归，用户旧库、实际内容规模、物理设备与用户验收。

E8c1 不代表整个 E8c 完成；E6、L3d3、L4d2、E8a3b、E8b3b 的既有门槛也不因此关闭。E9 仍是按需立项候选，不在本次实现中扩展。

## E8c1 验证记录

2026-09-12：

| 验证 | 结果与边界 |
| --- | --- |
| 双端共享契约 | 23 组：排序、分页、删除排除、归档/完成、中文、字面字符、Unicode 边界、摘要与历史排除 |
| 桌面 Rust | 搜索专项 3 项通过；完整 lib 测试 266 通过、11 个外部 S3 用例按原配置忽略，本轮未重跑这些云端用例 |
| 桌面 API/前端 | 搜索 API 3 项通过；完整 Vitest 299 项通过；svelte-check 0 错误/0 警告；生产构建和 Rust 格式检查通过 |
| 鸿蒙宿主 | 生产 Repository 运行在真实宿主 SQLite 适配器；120 条近上限正文三页、目标实时复核、五类无效/部分结果集注入、事务回滚及失败脱敏通过 |
| 既有宿主适配器 | 增加 rowCount 返回值后，47 组重复协议及持久化/迁移/CAS/回滚回归通过 |
| ArkTS 静态检查 | 修改的 Repository 与原生测试文件 0 错误；异常处理建议仍存在，异常由外层事务包装捕获，不将警告写成全部消除 |
| 手机模拟器 | Mate 80 Pro Max / 127.0.0.1:5557，Debug 主包和测试包构建、保留数据覆盖安装、ContentSearchNative 4/4 通过 |
| 平板模拟器 | MatePad Pro 13 / 127.0.0.1:5555，同一原生套件 4/4 通过 |
| 原生测试范围 | 三类检索、归档/完成、字面与 Unicode、55 条长正文分页、失效目标、缺表故障和失败后重试；使用并清理专用临时库，不改用户数据 |

原生证据目录位于本机临时目录，未提交构建产物：

- 手机：`%TEMP%/eggdone-device-tests-c7eaa3ffee714ecdb759f7dfda03146d`。
- 平板：`%TEMP%/eggdone-device-tests-5d4da0a53e7446a089c3c3eaee35f75d`。
- 各目录包含 default/ohosTest 构建日志、HAP SHA-256、覆盖安装日志与 Hypium 报告。
- 修复前缺表用例曾失败，修复后同一断言通过；没有通过卸载、吞掉错误或降低断言规避失败。
- 模拟器原生数据库测试不是物理设备、旧用户数据库、搜索界面或跨端实际操作验收。

## E8c2-D 桌面交付

- 入口：首页「更多 → 统一搜索」，以及已保存便签编辑器的「统一搜索」。现有任务/便签局部筛选保留，不改变主视图、分组和筛选条件。
- 输入后按 Enter 或点击搜索；按任务、便签、附件名称分组显示摘要，每组独立分页，可用结果类型选择框只查看一类。修改输入清除旧结果，清空不列出全部资料。
- 点击结果先复核当前内容：普通任务/便签进入现有编辑界面；已归档任务仅展示完整只读内容；附件打开所属便签的管理面板并滚动、高亮目标，不自动打开或下载文件。
- 搜索会话期间暂停后台列表卡片的预览读取；关闭搜索恢复正常卡片行为。搜索导航不读取附件原图/预览，用户主动打开附件仍走现有流程。
- 来源便签先保存，保存失败保留输入并阻止跳转；返回搜索保留词、结果类型、各组页及滚动位置。编辑目标后保存失败同样不能返回。
- 历史、关联和搜索入口互斥；搜索中暂缓原生快捷收集请求，退出后重新读取。搜索结果进入历史恢复后若刷新失败，关闭过期编辑器并重新显示搜索，不回填恢复前旧文。
- 短窗口采用整面板滚动并固定关闭区，较高窗口只滚动结果；宽窗口限制阅读宽度，长标题和英文按钮自然换行，不缩小字体来隐藏溢出。

自动化使用真实 Svelte 组件、保存队列及提取的生产导航/预览逻辑，数据库 IPC 为隔离替身，不接触用户数据。这些证据不替代 Tauri 原生 IPC/窗口、用户旧库、真实规模与物理设备验收；鸿蒙本轮仅同步计划，不能当作已具备搜索入口。

2026-09-12 验证：

| 验证 | 结果与边界 |
| --- | --- |
| 搜索状态单测 | 7 项：输入规范、空白/超长/NUL、过期响应、并发翻页、分组失败/重试、清空/销毁、页范围 |
| 完整前端 | 35 个测试文件、306 项通过；中英文 723 个键及参数一致；svelte-check 0 错误/0 警告，生产构建通过 |
| 搜索界面 | 24 组：中英文、深浅主题、320/480/1100 宽窗口、100%/150% 缩放；转义文本、归档只读、任务/便签/附件导航与上下文恢复通过 |
| 搜索故障与 I/O | 来源保存失败/冻结、目标编辑返回失败、目标失效、部分查询失败/重试；搜索不读取原图/预览，退出恢复列表卡片预览；嵌套返回不重开已关闭的附件管理器 |
| 既有回归 | 历史界面 24 组、关联导航 24 组通过；历史生命周期和关联导航宿主检查通过，新增历史恢复刷新失败后重新显示搜索的回归 |
| Rust 构建 | 格式检查及 cargo check 通过；既有 TraySnapshot.locale 未使用警告保留，本阶段未改 Rust，不将前一阶段 lib 测试记录当作本轮重跑 |

界面截图和隔离测试记录位于本机临时目录，未加入提交。需要复核当前界面时应重新运行脚本，以最新输出目录为准。

## 复跑命令

桌面工程：`pnpm test`、`pnpm check`、`pnpm build`；在 src-tauri 下运行 `cargo test --lib`、`cargo fmt --all -- --check`。

桌面界面：`node scripts/test-content-search-ui.mjs`、`node scripts/test-note-history-ui.mjs`、`node scripts/test-link-navigation-ui.mjs`；宿主编排：`node scripts/test-note-history-lifecycle.cjs`、`node scripts/test-link-navigation.cjs`。浏览器脚本需要 Playwright 和 Edge，允许用环境变量 `PLAYWRIGHT_PATH` 指向已安装的 Playwright；与 `pnpm check/build` 串行运行，避免配置刷新打断页面。

鸿蒙仓库：`node scripts/test-content-search.cjs --desktop=D:/Develop/EggDone`、`node scripts/test-recurrence-protocol.cjs`。
先 `devecocli device list` 确认序列号，再使用 PowerShell 7 执行 `scripts/run-device-tests.ps1 -Device 127.0.0.1:5557 -Suite ContentSearch`（平板改为当前平板序列号）。脚本串行构建并覆盖安装，不卸载应用；只对该套件专用临时库做清理。
