# E5a 双端运行测试记录

## 最新用户确认

2026-09-11：用户在附件修复后明确回复“真机验收通过”，本阶段用户验收收口，随后授权开始E5b任务短时撤销。未逐项提供机型、图片用例及全部语言/主题/字体/异常网络组合，不据此扩大覆盖范围。下文“待真机/用户验收”等描述保留为此前测试时的历史记录。

日期：2026-09-11。初轮只测试与记录；随后修复问题2。用户补充真机也存在附件导入失败后，已继续定位并修复问题1。用户随后授权生成handoff并本地提交，提交记录见交接文档；未推送或发布。

## 环境

- 桌面：Windows真实Tauri WebView2与SQLite。以当前源码编译，使用临时构建配置 `identifier=com.eggdone.e5atest` 隔离用户数据库；未改工程配置文件。
- 桌面构建最后复制默认exe时遇到旧进程占用。实际运行本次编译生成的 `src-tauri/target/debug/deps/eggdone.exe`，测试目录为 `%APPDATA%/com.eggdone.e5atest`。不是可发布包，不替代正常构建。
- 鸿蒙：Mate 80 Pro Max手机模拟器 `127.0.0.1:5555`，MatePad Pro 13平板模拟器 `127.0.0.1:5557`。当前Debug包覆盖安装成功，未卸载、未清数据。
- 鸿蒙通过系统UI自动化控件树及点击/输入操作验证；没有连接真实手机或平板。模拟器保留原同步配置，测试便签会按原设置同步。

## 通过项

| 场景 | 桌面 | 手机模拟器 | 平板模拟器 |
| --- | --- | --- | --- |
| 空白新便签直接关闭，不新增记录 | 通过 | 通过 | 通过 |
| 输入标题正文，完成后重新打开 | 通过 | 通过 | 通过 |
| 便签/任务视图往返后正文保留 | 通过 | 通过 | 通过 |
| 编辑后进入设置，再返回编辑器 | 未专项操作 | 未专项操作 | 通过 |
| 仅添加Markdown附件，关闭后重新打开 | 通过 | 未通过，见问题1 | 未专项操作 |
| 实际本地写入失败时保留编辑器与文字 | 通过 | 宿主注入测试 | 宿主注入测试 |
| 故障解除后点重试，不重新输入，重新打开内容一致 | 通过 | 宿主注入测试 | 宿主注入测试 |
| 桌面WebView重新加载后从SQLite恢复文字 | 通过 | 不适用 | 不适用 |

桌面故障验证仅在隔离数据库创建临时测试触发器，使正文UPDATE失败。失败时数据库仍是旧正文，编辑器保留新正文并显示“重试”。移除触发器后点击重试，重新打开及只读SQL查询均确认新正文已保存；测试触发器数量已核对为0，没有改用户数据库。

手机/平板正文同时出现预期的测试便签，说明本次正常网络下既有同步链路传递了测试内容；不等同离线、冲突或附件同步完整验收。

## 问题1：手机文件导入失败（已修复，待真机复测）

- 新建空便签，添加文件，通过系统选择器选择生成的 `E5A-QA-attachment.md`。
- 界面显示“附件处理未完成。请确认文件仍可访问后重试。”
- 自动生成了文件名对应的便签标题，但没有附件；在已创建的同一便签中重新选择同一文件，仍失败。
- 文件是64字节的无用户内容测试Markdown，通过HDC放入模拟器Download目录，系统文件选择器能列出并选中。
- 无应用崩溃日志。当前应用级错误日志没有提供足够的原始文件异常，不能断言属于E5a回归，也不能排除模拟器文件来源/访问授权影响。
- 此项最初按用户要求暂缓；用户随后确认真机也能复现并授权分析修复。以下补充覆盖原暂缓结论。
- 截图：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-e5a-qa/phone-attachment-failure.png`。

### 附件根因与修复验证

- 在手机模拟器中使用同一个64 B Markdown文件复现。补充不含URI、文件名、内容或凭据的阶段日志后，捕获 `NoteAssetImport: file stage=prepare-root code=13900015`。
- 文件还未读取，更未上传云端，就在 `fileIo.mkdir(assetRoot, true)` 失败。Harmony递归创建目录仍可能对已有目标返回File exists；导入图片、导入文件均存在相同调用。
- 图片与文件共用 `ensureAssetRoot()`：只接受错误码13900015且目标确实为目录的情况。保留权限、非目录、UUID冲突、大小与内容校验等错误，未申请新权限、未清理用户资产。
- 12项生产服务宿主测试通过；修改前“已有根目录”测试以13900015失败，修改后通过。覆盖首次/重复导入、并发目录创建、同名普通文件、权限失败、复制失败清理、无效内容、超限和UUID保护。
- DevEco Debug构建成功；MCP检查0错误、40条异常处理提示。保存生命周期16项回归通过。
- 修复包覆盖安装到手机模拟器；同一文件连续导入两次，附件数量0→1→2，两项均显示64 B并最终为“本机可用”。
- 平板模拟器沿用上一轮包，收到两项文件元数据；点击第一项“保存”成功下载原文件并打开系统保存选择器。取消导出后第一项显示“本机可用”，第二项仍为“原文件待下载”，验证本次正常网络下上传与下载链路。
- 手机图库没有可选测试图片，因此图片端到端导入尚未验证；本次未连接真机，用户报告的真机环境仍需用修复包复测。不将模拟器成功等同真机全部文件来源通过。

## 问题2：平板便签数量未实时刷新（已修复）

- 停留便签工作区，手机端新增便签同步到平板后，列表出现4张便签，左侧统计仍显示2。
- 从平板返回任务视图，再进入便签后，统计恢复为4。
- 本机创建首张测试便签时也观察到列表增加但侧栏数量仍为旧值。
- 原因是侧栏使用按值传参的 `@Builder StatCard`，数量参数是当时的快照，后续列表变化没有驱动数字刷新。新增 `NoteStatCard`，使用 `@Prop value` 接收当前总数及置顶数，保留原有样式；任务统计未改。
- 截图：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-e5a-qa/tablet-stale-count.png`。

### 修复后运行验证

- 新Debug包仅覆盖安装到平板模拟器，未卸载或清数据。手机模拟器保留初轮测试包，作为同步来源。
- 始终停留在平板便签工作区：新建后总数5→6；置顶0→1；取消置顶1→0；删除测试便签后总数6→5。
- 再次创建测试便签，删除后点击撤销，总数6→5→6立即更新。收起键盘后操作删除，不将键盘遮挡底部操作误判为功能故障。
- 手机新增另一张测试便签，平板未操作或切换页面，既有同步使总数6→7，列表及统计一致。
- 修复截图：`C:/Users/caozhipeng/AppData/Local/Temp/eggdone-e5a-qa/tablet-count-fixed.png`。

## 自动化复核

- 桌面 `pnpm test` 239项通过，生产编辑器宿主测试10项通过。
- 鸿蒙保存生命周期16项、工作区504布局+8编辑场景、导航10项通过。
- 两个鸿蒙模拟器的应用崩溃日志查询均无记录。
- 本次修复新增7项生产计数场景及响应式绑定检查通过；保存生命周期16项、工作区504布局+8编辑场景、导航10项回归通过。DevEco Debug构建成功，源码检查0错误、1警告。
- 问题1和问题2已通过上述模拟器实际运行验证；完整E5a用户验收、图片导入和真机矩阵仍未完成。

## 保留数据与边界

- 桌面测试仅写隔离库，保留 `E5A-QA-desktop-0911`、`E5A-QA-attachment` 供复核；测试进程结束，原桌面客户端未中断。
- 鸿蒙创建了 `E5A-QA-tablet-0911`、`E5A-QA-phone-0911` 和导入失败留下的 `E5A-QA-attachment`，未删除，可能已同步到其他设备。没有修改原有便签。
- 本次保留 `E5A-QA-count-live-0911` 和 `E5A-QA-count-sync-0911` 两张新测试便签，可能已同步到其他设备。删除与撤销仅操作本轮创建的测试便签，未修改用户已有记录。
- 附件修复另建 `QA-attachment-fix-0911`，保留两个相同64 B Markdown测试附件，已按原同步配置传递至平板；未修改或删除用户便签。
- 测试文件及桌面截图保留在上述临时目录。未修改产品版本号，未推送。
- 真机、图片端到端导入、完整语言/主题/字体/分屏矩阵、强杀恢复仍未验收；不继续下一开发阶段。本次业务代码仅修改鸿蒙端，桌面端只同步测试记录。
