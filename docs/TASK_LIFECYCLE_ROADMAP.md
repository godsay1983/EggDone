# 双端任务安排与归档回收管理 Roadmap

日期：2026-09-19（LC4 界面及计划/等待跨端核心联动用户验收通过；LC5 综合自动化完成，进入发布整理）。
范围：归档管理与恢复、安全清空回收站、今日计划、等待处理。
方案：[TASK_LIFECYCLE_IMPLEMENTATION_PLAN.md](TASK_LIFECYCLE_IMPLEMENTATION_PLAN.md)。

## 当前检查点

- [x] 用户授权桌面 1.3.0、鸿蒙 1.4.0 / 1000031 升版及交接、本地提交。元数据、CHANGELOG 和 README 已同步；本次版本/构建检查见[综合回归](TASK_LIFECYCLE_REGRESSION.md#升版验证)。不新增迁移、不推送或正式发布。
- [x] 2026-09-19 用户确认等待界面和任务菜单修复测试无问题；本地提交桌面 `7212bdc`、鸿蒙 `564415c`，未推送。
- [x] LC5 新增双端各 4 条生产命令/仓储完整流程测试；桌面 Rust 481 通过、40 忽略，鸿蒙 22 个相关脚本通过。见 [综合回归与剩余验收](TASK_LIFECYCLE_REGRESSION.md)。本轮新增测试与文档按用户要求纳入本次本地提交，未推送，未更改产品逻辑、协议或版本。
- [x] 用户确认桌面加入计划并设为等待 -> 鸿蒙同步显示双标签 -> 鸿蒙恢复处理 -> 桌面同步后仅保留今日计划的往返通过。回归提交为桌面 `660f3b6`、鸿蒙 `eb68078`，未推送。
- [ ] 未逐项确认的跨日/并发和发布设备矩阵保留，不重复已确认的 UI 与跨端往返测试，不新增迁移步骤。以下早期“未提交”等表述保留作历史记录，不覆盖此检查点。

- [x] 2026-09-19 今日计划完整实现、自动接续同步位置及 HTTPS 历史绑定兼容修复、任务卡片/菜单统一、今日计划标签与高级路径设置纳入本次本地提交。未推送、未升版或发布；不自动修改用户存储配置。

- [x] 2026-09-19 同步空间入口从两端回收站移入同步高级设置；用户明确反馈验证无问题，本轮纳入本地提交，未推送。
- 桌面附件条件删除签名与待清理状态修复已提交 `1b53e75`；双端直接清空基线为桌面 `f23b13c`、鸿蒙 `81a68e6`。后续不再把迁移工具开发作为清空的前置工作。
- [x] 用户授权继续 LC3 并使用子代理；双端今日计划已完整接通界面、存储、同步、备份和恢复。schema 25，规划域非空的备份 v7；无需同步空间迁移，纳入本次本地提交，未升版。实现和验证见 [DAILY_PLANNING_PROTOCOL.md](DAILY_PLANNING_PROTOCOL.md)。
- [ ] LC3 手机、平板、桌面原生操作及用户云存储验收。未覆盖的 LC2 实际附件清理矩阵仍保留，不因本轮自动化自动勾选。

- [x] 2026-09-19 产品方向调整：清空不再依赖迁移或全空间备份。双端单项/批量/清空一次确认，本机先删除，网络同步及附件清理后台续作。
- [x] 原空间新增独立删除记录，正常同步先合并终态再同步正文；保留已迁移空间兼容，不自动切换用户目标。
- [x] 原空间双向隔离 S3 验证通过：本机无网络删除、旧副本过滤、附件删除回复丢失后续作；已迁移空间双向回归也通过。
- [ ] 更新两端后完成原生桌面、手机/平板和用户实际存储验收。所有同步设备需升级，不支持新旧版本混用。
- 当前流程以 [TASK_DIRECT_PURGE.md](TASK_DIRECT_PURGE.md) 为准；以下迁移准备验收不再是清空的前置门槛。本轮未提交、未升版或发布，不开始 LC3/LC4。

### 迁移方案历史检查点

- [x] 桌面旧库恢复失败修复：以 schema 23 补齐早期 v22 缺失的 local_attempts，保留已存在计数；旧快照按已知字段名映射恢复，拒绝其他缺列/重复列，仍严格检查内容、引用和附件。
- [ ] 更新后重新验收桌面“检查并准备”；本次不改变鸿蒙运行逻辑、不自动切换用户同步空间或清空资料。

- [x] 2026-09-19 验收修复：历史已删除附件 404 可在明确核对缺失清单后继续迁移；活动附件、权限/网络/完整性错误仍阻止。双端保留缺失证据及旧空间，参与设备需更新此次修复，见[边界与验证](TASK_PURGE_ACTIVATION_ACCEPTANCE.md)。
- [ ] 此修复的原生桌面、手机/平板及用户存储复验；未执行真实迁移/清空，未升版或发布。当前修复尚未提交，不开启 LC3/LC4。

- [x] 双端普通入口已贯通：快照/暂存、同来源目标登记、八域与终态空间初始化、原子激活、另一设备加入、同步用户清空授权。
- [x] 清空 UI 显示本机移除、待同步、云端附件待清理/重试和完成；空回收站也可进入迁移准备。
- [x] 双向隔离 S3 使用正常准入验证迁移、清空、断网/回复丢失续作和旧副本不复活；没有替换生产保护。
- [x] Rust、前端、鸿蒙协议/存储/面板自动化与双端构建。见[本轮完整证据](TASK_PURGE_ACTIVATION_ACCEPTANCE.md)。
- [ ] 原生桌面、手机/平板及用户实际存储验收。未执行的矩阵不以 host 测试代替。
- 代码尚未提交、未升版或发布；未操作用户资料。LC2 进入验收，不再拆迁移后台步骤，不提前做 LC3/LC4。
- 保留旧空间、备份和未激活新空间保护；迁移不是历史副本擦除。下文未勾选的旧阶段条目保留作历史记录，以本段和验收文档为准。

## 历史检查点

以下是历史快照。诸如“本轮未提交”“下一步”的相对措辞只对应当时记录，不覆盖上面的当前状态与交付审计。

- [x] 双端生产传输层的新空间八域封装及普通运行入口保护已实现（本轮未提交），共享样例、HTTP 边界和隔离真实 S3 双端往返通过。无新界面、无用户空间切换，见[实现与验证](TASK_SYNC_SPACE_PROTOCOL.md)。
- [ ] 激活前仍需运行空间初始化/领域约束、发布仲裁、原子切换与恢复、设备加入、终态生产同步及远端清理。普通新空间同步及旧空间永久删除保护保留，LC2 未完成。
- 上一轮暂存发布已提交：桌面 f8c10f3，鸿蒙 d14afef；收尾时 origin/main 已对齐，本轮未执行推送。下方历史检查点不覆盖本段状态。

- [x] 双端原生迁移暂存发布及独立确认界面已实现（纳入本次本地提交）：固定目标、条件创建、逐文件读回、持久进度、故障重试与冲突停止，见[实现、证据与边界](TASK_MIGRATION_PUBLICATION.md)。
- [ ] 暂存不是活动空间；下一步连接正式新空间协议、发布仲裁、事务切换/重启恢复和设备加入，然后接终态同步与远端清理。LC2 完整功能仍未交付。
- 上一轮云端快照已本地提交：桌面 59fdbbe，鸿蒙 ce36652；未推送。

以下保留历史检查点，其“下一步”按上述当前检查点更新。


- [x] 双端原生只读云端快照已接入迁移准备入口：八域原始字节、强 ETag/不存在状态、全部引用附件、持久记录、本机来源绑定和重读复核；新增内容纳入本次本地提交，见[实现与证据](TASK_MIGRATION_CLOUD_SNAPSHOT.md)。
- [ ] 此快照不等于完整迁移日志或源空间冻结；下一步接目标新空间发布、CAS/读回/崩溃续作与切换加入，之后接终态生产同步及远端清理。LC2 尚未完成。
- 前一轮恢复/附件安全已提交：桌面 ab2b67c，鸿蒙 1dde327；未推送。

- [x] 双端迁移快照缺失附件只读下载、源变化拒绝与可恢复重试已接入；现有远端附件清理增加强 ETag 条件删除，隔离真实 S3 替换竞态通过。纳入本次本地提交，见[附件安全增量](TASK_PURGE_ASSET_SAFETY.md)。
- [ ] LC2 完整功能仍未交付：正式迁移/加入、终态生产同步与云端清空队列尚未接通，旧空间永久删除保护继续保留。

- [x] 双端“准备快照/重新校验”接入隔离建库、事务恢复、重开比对及附件目录恢复校验；仅开发与自动化完成，纳入本次本地提交，见[恢复验证与边界](TASK_MIGRATION_RECOVERY_DRILL.md)。
- [ ] 当前源库替换/正式回滚、强杀后的临时资料回收及原生设备恢复验收不在本轮完成范围。

- [x] 双端新增原生本机恢复快照、逐文件落盘重验、同 ID 重试/重开和“迁移准备”界面；包含已删除附件、历史、回执与终态/清理队列。纳入本次本地提交，见[实现与限制](TASK_MIGRATION_NATIVE_BACKUP.md)。
- [ ] 该快照不是可跨端导入的备份；内部隔离恢复与缺附件下载已接入，云对象备份绑定、正式迁移/加入、终态云同步与远端清理队列仍需完成；云端也已丢失的附件继续阻止完整快照。

- [x] LC2 本机后端与双端入口已实现，纳入本次本地提交：固定目标、50 项原子批次、终态保护、暂停/重启续作、附件清理重试、备份 v6 条件升级和双端互导。仅未配置过同步的设备可执行，见[当前证据与缺口](TASK_PURGE_NATIVE_PROGRESS.md)。
- [ ] LC2 整体仍未完成：正式迁移、生产终态云同步、远端清理与原生设备验收不能由本机自动化替代。
- 用户要求后端、同步和界面作为一个完整功能连续推进；不再每个内部子步骤结束就要求重新确认。没有用户真实数据迁移授权。

- LC2a-2b-1 已本地提交：桌面 ca30607、鸿蒙 330b411，未推送。
- [x] LC2a-2b-2 本机子项：两端只读事务盘点、生产 dirty/ACK/revision 和附件门槛、过期快照拒绝；37 组共享用例和全量 S3 回归通过。新增内容未提交，见[实现与边界](TASK_MIGRATION_LOCAL_PREFLIGHT.md)。
- [ ] LC2a-2b-2 剩余：实际完整备份证明、已删除附件/本地历史保护范围、与持久迁移计划绑定及最终切换前复核。

- LC2a-2a 已本地提交：桌面 febaa21、鸿蒙 392e4a7，未推送。
- [x] LC2a-2b-1：接入两端生产解析器及固定测试路径派生、存储目标绑定，六阶段隔离真实 S3 复制/发布/日志恢复和晚到写入验证通过，已提交。见[证据与边界](TASK_MIGRATION_S3_VALIDATION.md)；正式导入仍未接入。
- 前一轮已本地提交：桌面 d9dad0a、鸿蒙 4925b30，包含 LC1c 同步回归及 LC2a-1 兼容原型；本次未推送。
- [x] LC2a-2a：盘点 8 个云元数据对象/9 类记录及附件引用；新增原始字节备份、确认摘要、SQLite 进度与目标切换事务。host 原型及进程终止恢复已实现，新增内容纳入本次提交。见[迁移预览与持久恢复](TASK_MIGRATION_JOURNAL_PROTOTYPE.md)；不是正式迁移入口。
- [x] LC1c 自动化补齐：双端生产归档/恢复与完整同步服务经隔离真实 S3 四阶段往返通过，覆盖离线、错误凭据、冲突、重复调用和旧副本重连；原回收站 S3 套件回归通过。纳入本次提交，见[证据与边界](TASK_ARCHIVE_SYNC_ACCEPTANCE.md)。
- 用户已确认归档跨端同步、取消归档/重新打开及旧提醒/重复保护的剩余验收范围，LC1c 本次业务范围收尾；未逐项说明的主题、字号和设备矩阵仍保留，不重复已验收基础操作。
- [x] LC2a-1：候选终态合并及任务单域迁移原型完成；29 项模型检查、125 组合并结合性、14 个 I/O 故障注入点及四阶段隔离真实 S3 通过。旧端使用当前未扩展协议的生产同步内核，新端为 Node 原型，不代表原生迁移交付。见[兼容原型](TASK_PURGE_COMPATIBILITY_PROTOTYPE.md)。
- 用户已接受“显式迁移、参与设备升级、预先汇合离线修改并保留旧空间”；旧版后续写入不自动进入新空间。此确认不授权迁移实际数据，LC2a 总门槛仍未完成。
- [x] LC0a：核对双端基线、用户反馈和现有归档/回收站/搜索边界；写入共享实现方案及本 roadmap。
- [x] LC0b：共享契约、候选规则和 fixture 完成；两仓库分别通过 69 项候选检查及 125 组集合结合性组合，逐字节镜像一致。
- [x] LC1a：双端归档存储内核及自动化完成，纳入本地提交；详见[实现与验证](TASK_ARCHIVE_STORAGE.md)。
- [x] LC1b-1：双端已归档入口、列表搜索、详情和单条操作已实现；纳入本地提交，原生/真实同步验收未完成。见[入口与验证](TASK_ARCHIVE_UI.md)。
- [x] LC1b-2a：批量选择、固定目标确认、结果明细、中断恢复与结束保护已实现并纳入本地提交，见[批量入口与验证](TASK_ARCHIVE_BATCH_UI.md)。
- LC1b-2a 已本地提交：桌面 2417af2，鸿蒙 df98095；未推送或发布。
- 用户反馈插入项：双端归档/回收站紧凑列表、左侧勾选、顶部搜索与批量工具栏已实现，按用户要求纳入本地提交，原生视觉验收范围未另行确认。见[列表优化](TASK_MANAGEMENT_LIST_UI.md)；不改变后续功能顺序。
- [x] LC1b-2b：统一搜索归档过滤与恢复、删除文案、详情补齐和列表层次已实现，纳入本地提交；见[实现与验证](TASK_ARCHIVE_SEARCH_UI.md)。下一步 LC1c 原生与真实同步验收；数据库 schema、同步 wire、备份及应用版本未改。
- LC0a 已本地提交：桌面 76363b2，鸿蒙 4379e61；LC0b 已本地提交：桌面 0d4b6c7，鸿蒙 6c8cc48；未推送或发布。
- LC1a 已本地提交：桌面 fa8dbab，鸿蒙 f7d7223；未推送或发布。
- LC1b-1 已本地提交：桌面 32a2d20，鸿蒙 47af21e；未推送或发布。
- LC1b-1：桌面 403 项 Vitest、24 组隔离 UI 场景、类型检查及构建通过；鸿蒙面板/Store host 测试及 Debug 构建通过。未将 host 测试算作原生 RDB 或真机验收。
- LC1a：桌面 check/build/fmt/check 通过，Rust 341 项通过、16 项按条件忽略；归档跨端交换测试另行显式执行通过。鸿蒙 host SQLite 23 个共享用例及升级/导入/跨端回归通过，Debug 和 ohosTest 构建通过；原生测试仅编译，未设备执行。
- 桌面基线 main / a5fe0b0 / 1.2.0 / SQLite 21；鸿蒙 main / c227c3f / 1.3.0 / 1000030 / RDB 22；备份 v5。
- 本文与实现方案在两仓库保持内容一致。阶段结束先更新准确状态和证据，再提出下一子阶段。

## 顺序与完成标准

LC0b -> LC1 归档管理 -> LC2 安全清空 -> LC3 今日计划 -> LC4 等待处理 -> LC5 收尾。
LC2 按一个完整功能连续实施，内部检查点只用于记录证据，不等于产品交付。同步兼容门槛未通过时保持旧空间阻断，不以隐藏记录替代清空，不跳到 LC3。

每个阶段区分三个结果：内核/自动化完成、产品入口完成、用户/原生验收完成。只做内核时明确告知界面未变。
既有功能用户已确认的基础验收不重做；只补本轮影响面和未验证组合。

## LC0b：冻结共享契约与测试样例

- [x] 状态转换表：取消归档与重新打开、等待与计划正交、父任务不可用优先、复制/新重复实例不继承安排。
- [x] 冻结归档命令、预览版本快照、批量目标及冲突错误；盘点恢复路径和已知数据库触发器。
- [x] 冻结计划与等待生命周期栅栏：旧计划/等待在父任务完成、删除及恢复后不能迟到复活。
- [x] 编制 LC2 旧端/新端/离线设备/备份兼容矩阵及同步隔离原型验收用例；当前已接本机入口，旧同步空间仍不开放执行。
- [x] 双端共享 fixture 文件逐字节相同，并分别运行候选纯规则验证（不是生产 adapter）。
- [x] 明确每阶段迁移与备份影响，不提前修改 app 版本或分配未使用的 schema。
- [x] 更新说明，单独报告契约完成，不声称功能已交付。

产出：[共享契约](TASK_LIFECYCLE_CONTRACT.md)、[fixtures](fixtures/task-lifecycle-v1.json)、scripts/task-lifecycle-reference.cjs、scripts/test-task-lifecycle-contract.cjs。无产品入口，不操作用户数据。
证据：两端各执行 node scripts/test-task-lifecycle-contract.cjs --peer=另一仓库，通过 69 项候选检查；fixture 含 25 项归档动作和 25 项状态投影，另有日期/长度/合并代数/固定目标检查。持久批量事务、回执、完成历史 UI、旧端隔离与真实传输未实现，仍属后续门槛。

## LC1：归档管理与恢复

### LC1a 内核

- [x] 双端归档列表/搜索/分页/详情，稳定排序和参数校验。
- [x] 取消归档保持完成状态；重新打开变为单次未完成任务，保留清单勾选和有效关联。
- [x] 提醒/重复保护、活动规则当前实例冲突、失效分组处理；旧式重复任务重新打开保留全天日期语义。
- [x] 从归档移入回收站；批量取消归档和批量删除的固定快照、结果明细。
- [x] 新库/前一 schema 重建升级库、事务回滚、版本冲突、重复调用、跨端生产 JSON 往返回归（不等于原生/S3）。

复用 app_metadata 保存本机 scope、无正文操作回执及固定批量进度；导入备份在同一事务使旧预览/回执失效，同步空间切换由现有 epoch 隔离。LC1b-1 已接命令/Store 层权威时间和设备身份、当前状态读取及提交后刷新；LC2/3 的终态和生命周期栅栏尚未实现。

### LC1b-1 单条管理入口

- [x] 桌面/手机菜单及平板侧栏相邻放置已归档、回收站；不添加新的主导航。
- [x] 列表搜索、分页、只读详情、单条动作、空/加载/失败/冲突状态。
- [x] 单条动作二次确认、固定请求重试、提交成功但刷新失败时仅重试刷新。
- [x] 完成隐藏时提供查看入口，临时显示已完成但不保存该偏好；快捷键/系统返回/取消不写入。
- [x] 两端类型/静态检查、构建；桌面隔离 UI 自动化及鸿蒙 host 面板测试。

### LC1b-2a 批量管理

- [x] 明确选择/全选已加载、固定目标确认、结果明细与失败重试界面。
- [x] 中断或最终回复丢失后发现原批次，用户确认继续；停止需二次确认，保留已成功结果。
- [x] 两端提交后刷新、空间隔离、导入清理结束标记、重复点击与返回保护。
- [x] 桌面 409 项 Vitest、343 项 Rust、24 组单条加 4 组批量 UI 和最终回复丢失恢复；鸿蒙 host 仓库/Store/面板、Debug 构建。均不代表真机或 S3 验收。

### LC1b-2b 搜索与详情收敛

- [x] 统一搜索含归档开关及恢复动作，复用同一服务，保留搜索上下文；恢复后刷新当前页，末页为空时退回最后有效页。
- [x] “清除已完成”改为“删除已完成”，确认说明进入回收站。
- [x] 归档详情有效关联入口、原日期已过提示与列表分组；关联便签首版只读文字预览，不加载附件或进入编辑器。
- [x] 双端检查、构建和自动化；列表轻底色/细边框/间距及选中反馈一并完成，实际设备验收不在此项内。

### LC1c 验收

- [x] 完整同步自动化：桌面 Rust / 鸿蒙生产 ArkTS host + 隔离真实 S3，双向归档恢复、离线重试、错误凭据恢复、预览冲突、旧副本重连与日期/清单/关联保护通过；不代表原生 ArkData 和系统提醒验收。
- [x] 鸿蒙详情单行底栏用户验收通过（edae65c），此前其他测试正常的反馈已记录；具体未说明的设备和组合不自动勾选。
- [ ] 桌面与鸿蒙手机/平板实际操作，检查窄窗、键盘、大字体和深浅主题。
- [x] 双端归档/取消归档/重新打开后的实际同步及旧提醒/重复保护：用户已针对剩余验收范围确认通过；自动化隔离同步证据另见验收记录，不推定用户采用的存储服务。
- [x] 用户确认并记录具体范围，更新文档；未测试矩阵继续列为未验收。

LC1 完成后用户应能独立管理归档，不再只能通过搜索发现历史任务。

## LC2：安全清空回收站

### LC2a 协议与旧端兼容原型（阻塞门槛）

分步推进：LC2a-1/2a/2b-1 已提交；LC2a-2b-2 本机待同步内容、附件及修订门槛已实现，实际备份证明和执行器接线待完成。随后补全正式映射/依赖/导入并进入 LC2a-3 原生及已发布旧版客户端验收。下列总门槛未因原型或本机检查通过而全部完成。

- [ ] 冻结终态删除凭据 schema、冲突优先级、Object Key/协议世代、最小元数据及非正文回执。
- [ ] 验证新同步空间/受控迁移，明确旧端如何隔离；仅依赖旧端忽略的版本提示不算通过。
- [ ] 定义离线修改汇合、备份预览、条件切换、中断恢复、旧空间保留和回退限制。
- [ ] 真实旧客户端晚到更新、恢复、旧快照上传不能使新空间记录复活。
- [ ] 旧备份恢复、首次设备加入、同步配置切换、本机模式以后启用同步的规则验证。
- [x] 用户已明确接受显式迁移并保留旧空间；需要升级参与设备、迁移前汇合离线修改，旧版之后的写入不自动进入新空间。清空入口仍未开放。

### LC2b 本地终态与物理清理

- [ ] 双端迁移、terminal/purge repository、查询投影和全部恢复入口拒绝终态身份。
- [ ] 固定整批目标、持久进度、每批事务、部分成功报告和幂等重试。
- [ ] 盘点正文副本：清单、等待原因、计划、便签历史、索引、创建重试负载、附件元数据。
- [ ] 保留关联的独立实体、独立模板及仍有用途的重复定义，不误删共享文件。
- [ ] 持久文件清理队列，验证进程终止、磁盘错误与重启继续。
- [ ] 必要删除凭据不按时间擅自过期，且不携带业务正文。

### LC2c 同步、备份与异常

- [ ] 新领域并入生产同步、独立 dirty/ACK、ETag 条件合并及配置世代保护。
- [ ] 终态凭据传播后再条件清理远端文件；离线/权限错误/超时可恢复。
- [ ] 新备份与导入规则，旧备份缺失领域不抹去现有终态凭据。
- [ ] 双端离线删除/恢复竞争、同步中断、旧空间写入和恢复过程失败测试。
- [ ] 明确逻辑不可恢复与物理清理范围，不能承诺清除独立备份或 S3 历史版本。

### LC2d 入口与验收

- [ ] 单条、选择删除和整个回收站清空，二次确认数量/附件/不可恢复提示。
- [ ] 确认后的新删除项不误清理，预览后已恢复项跳过并报告。
- [ ] 显示本机完成、待同步、远端清理失败和重试，不伪报全端完成。
- [ ] 桌面、手机、平板验收，真实隔离存储和离线设备重连验收。
- [ ] 用户确认语义与结果后收口；发布功能前 LC2a-c 必须全部通过。

## LC3：今日计划

### LC3a 数据与同步

- [x] 冻结 task_uuid + plan_date 逻辑身份、移出、排序、日期/时区和生命周期规则。
- [x] 双端迁移、repository、独立同步域和备份；不复用截止日期或全局排序。
- [ ] 跨日、另一时区查看、完成/恢复后迟到计划、并发排序及重试回归。

### LC3b 入口与体验

- [x] 任务更多加入/移出今日计划；今天页“计划做 / 到期与逾期”两种视图。
- [x] 计划内排序、当天完成折叠、昨天未完成继续安排，按任务去重。
- [x] 无日期任务可安排；不修改日期、提醒、分组、优先级或现有卡片语义。
- [x] 加载/空/失败/冲突/跨日刷新；入口复用紧凑菜单。浏览器窄窗自动化通过，原生设备视觉验收留在 LC3c。

### LC3c 验收

- [ ] 双端实际同步、跨日和失效父任务用例；时区行为单独记录。
- [ ] 桌面/手机/平板 UI 与用户操作验收。
- [ ] 原今天筛选、四象限、提醒、重复、清单无回归。

## LC4：等待处理

### LC4a 数据与规则

- [x] 冻结 ready/waiting、原因长度、查看日期、结束标记及父生命周期规则，见 [LC4 协议](WAITING_WORKFLOW_PROTOCOL.md)。
- [x] 双端 schema 26 存储、独立同步领域、备份 v8 和乐观版本校验；不修改原任务截止提醒。
- [x] 等待与移出当日计划的联合事务，取消零写入，失败全部回滚；故障注入已覆盖。
- [x] 完成/归档/删除结束等待，恢复/复制/新重复实例不继承；共享生命周期事件使旧远端等待失效。

### LC4b 入口与体验

- [x] 双端任务更多设为等待、原因/查看日期表单、恢复处理；等待列表筛选和排序，复用普通任务行。
- [x] 设等待时明确选择是否移出今日计划，默认不勾选。
- [x] 到查看日只标记需要复查，不自动解除或注册系统提醒。
- [x] 全部/今天到期/四象限保留截止风险，计划内等待标记与可执行数量一致，不修改普通未完成总数。
- [x] 空原因、无日期、长度校验、取消零写入、失败保留草稿/原样重试和跨日重试拦截已覆盖；真实键盘与返回体验留在 LC4c。

### LC4c 验收

- [x] 计划保留、设置等待和恢复处理的双端实际同步往返，用户已确认。
- [ ] 等待/完成/删除并发；原因及复查日期跨端一致和日期前后行为。
- [ ] 手机/小平板/大平板/桌面实际入口及状态可理解性。
- [x] 记录用户确认的界面及核心往返范围，更新说明，不以构建成功替代其余验收。

## LC5：收尾与发布准备

- [x] 生产命令/仓储自动化串联归档 -> 重新打开 -> 今日计划 -> 等待 -> 完成 -> 删除 -> 恢复/彻底删除；两端各 4 条，实际界面/云端组合验收不由此替代。
- [ ] 任务/便签/清单/附件/关联/模板保护，旧库升级、备份恢复和新旧同步空间回归。
- [ ] 提醒、重复后继、桌面卡片、小艺契约及实况窗核心无非预期变更。
- [ ] 桌面原生与鸿蒙手机/平板、分屏、中英文/主题/字号矩阵；未覆盖项如实保留。
- [x] 更新 CHANGELOG、README、使用与验收记录、handoff；注明升级前备份、同步设备一起更新和旧程序不能直接降级打开数据库。
- [x] 按用户明确授权升版并本地提交交接；当前已在 main，无需额外合并。
- [ ] 正式安装包、平台验收、推送或发布须用户另行授权。

## 下一步

2026-09-19：核心跨端往返已获用户确认，LC5 自动化已提交；本次按授权完成桌面 1.3.0 / 鸿蒙 1.4.0 升版与交接并纳入本地提交，见 [综合回归记录](TASK_LIFECYCLE_REGRESSION.md)。下一步等待用户指定正式打包的平台或发布范围；不自动开始新功能，不重复已通过验收，不操作用户数据或推送。其余设备/主题/并发/系统能力矩阵保留为发布风险。以下 LC3 记录是历史记录，不代表当前下一项。

LC3 实际同步验收发现的新旧位置分离已完成双端修复和隔离 S3 往返验证：正常同步自动接入可信既有目标，不再让用户选择空间或重新迁移。见 [AUTOMATIC_SYNC_TARGET.md](AUTOMATIC_SYNC_TARGET.md)。下一步更新双端后按正常同步操作验收；此方向取代下面历史记录中的手动加入入口。目标身份不明确或校验失败时报告错误，不盲目合并无关配置。

下一步是 LC3“今日计划”双端用户验收：任务更多加入计划、今天页查看与排序、跨端同步、完成后折叠、重开失效和昨天继续安排。代码、自动化与构建已完成；不修改原截止日期、提醒或桌面卡片语义。验收步骤见 [DAILY_PLANNING_PROTOCOL.md](DAILY_PLANNING_PROTOCOL.md)，不把自动化通过写作真机或用户云存储验收通过。

迁移/加入仅保留为兼容工具，不是清空前提；历史交付审计的拆分步骤不再作为当前“下一步”。未验证的原生/真实存储组合留在验收记录，若仍发现实际清理故障则优先修复；不会借新功能自动操作用户数据或切换空间。LC3 后依次为 LC4“等待处理”、LC5 综合回归与发布准备。
