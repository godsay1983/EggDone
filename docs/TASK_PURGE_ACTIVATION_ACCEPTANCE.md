# 清空回收站：正式迁移与验收

> 2026-09-19 更新：本文记录此前的迁移方案及已迁移空间兼容性。日常清空已取消迁移前置，请使用 [直接清空流程](TASK_DIRECT_PURGE.md)。不要为删除回收站项目重新执行迁移。

日期：2026-09-19。双端代码闭环已接通，提交信息见最新 handoff；未升版、未发布，真实用户空间未迁移或清空。
本文覆盖早期文档的“尚未接通”描述，不把自动化通过等同于真机验收。

## 用户流程

1. 参与设备升级后，先汇合旧空间的离线修改并完成同步。
2. 回收站 -> 迁移准备 -> 检查并准备。空回收站也保留入口。
3. 阅读并勾选旧空间保留、旧版后续写入不自动进入新空间的确认。
4. 首台设备选择“迁移并启用”；其他设备保留原来的同步配置，选择“加入新空间”。
5. 返回回收站，单项、勾选批量或清空全部。确认时固定目标，之后删除的新项目不顺带清空。
6. 显示本机移除、等待同步、云端附件待清理或本次完成。断网/权限错误保留进度，可同步并刷新进度、继续本机清理。

不要求用户修改 Object Key；手工填写 v2 路径不能取得激活权限。
迁移准备内部执行本机快照、缺失附件下载、隔离恢复、云快照和暂存，不再提供多个工程步骤按钮。

## 数据边界

- 固定来源映射：eggdone-space-links/v2/<旧主键 UTF-8 SHA-256>.json，条件创建、读回确认；不同候选不能各自覆盖同一来源。
- 映射 wire 为 [1, oldMainKey, seedManifestString]。来源指纹包含 endpoint、region、bucket、旧主键、path style 和 HTTP 选项，不含凭据。
- 新空间位于 eggdone-spaces/v2/<publication UUID>/。八域封装、引用文件和空终态账本全部核验后才写 ready.json。
- 登记来源前验证八域 schema、实际封装大小及活动附件/关联/清单/重复实例的父记录；软删除关系、停止规则和独立定义不因缺少旧实例而被擦除。
- 已 ready 的空间只验证当前文档，不重放种子，避免覆盖后续修改。文档/账本缺失、损坏或空间不符时停止，不自动补成空数据。
- 本机最终事务同时保存空间证明、主键、epoch、全部 ACK 失效和终态应用。切换失败回滚，回复丢失后按已保存状态续作。
- 加入前保留本机快照，核验并复制仍需保留的本机附件；先应用新空间终态，再允许本机旧内容参与正常合并。
- 本机模式已产生终态的设备可以加入已建立的新空间，保留并传播本机终态，不允许先向旧协议上传。
- 首台创建仍要求旧空间已同步稳定；已有本机终态且尚无目标空间时，不通过删除终态或伪造成功绕过检查，应由已稳定的设备先建立目标再加入。
- 附件证据在正文删除前保存，复用已有目标绑定和 If-Match 清理。真正改换存储目标不把旧清理任务重定向到新桶。
- 旧空间、暂存种子、迁移恢复资料、独立备份及对象存储历史版本不在清空范围。失败前复制的未引用文件也不在本轮自动回收范围。
- 同步进度采用保守判断：账本未确认、整体同步失败或仍有脏域时不显示全局完成；清理权限不足不能回滚已传播的删除。

## 本轮验证

- Rust 全量：400 passed / 33 ignored。忽略的存储集成测试需单独执行，不计作通过。
- 新增 Rust 4 项激活检查；鸿蒙协议覆盖 68 个请求前/响应后中断点、重试、不重放、缺对象拒绝、无效种子不登记和引用校验。
- 正式 S3 套件：scripts/run-sync-core-s3.ps1 -ActivationSessions -HarmonyRoot D:/Develop/EggDoneHarmony -Port 18480。
- 最终协议版双向证据：%TEMP%/eggdone-sync-core-dad389d1db2c4e61945e6aec91667c2f。
- 正向：桌面快照/发布/初始化/激活，鸿蒙完整服务加入、清空便签，删除成功但响应丢失后重试；桌面旧副本正式加入不复活。
- 反向：鸿蒙完整服务发起迁移，ready 写入回复丢失后新实例继续；桌面带本机删除终态加入，再清空便签与远端附件；鸿蒙旧副本重连不复活。
- 桌面使用正常准入、同步核心及正常成功状态记录，仅注入测试凭据；鸿蒙使用生产 SpaceActivationService / SyncService，仅适配主机文件、RDB、凭据和 HTTP。没有替换准入判断。
- S3 仅 loopback 临时服务和合成数据，正反向分桶。临时服务已删除。该证据不是用户 S3 兼容性或鸿蒙原生 RDB 验收。
- 前端 415 项、类型检查、翻译检查通过；桌面 Playwright 覆盖 24 个语言/主题/窗口/缩放组合及 4 组迁移/清空/失败续作。
- 最终界面回归包含激活后的回收站缓存刷新；截图：%TEMP%/eggdone-trash-ui-1789756352082。
- 鸿蒙清空/v6 备份、29 组远端清理、终态协议/事务/会话、40 组迁移预检、快照和目标门控回归；host 面板不等于 ArkUI 渲染。
- 桌面 Debug 可执行程序和鸿蒙 Debug HAP 已构建。保留既有编译警告，版本与数据库版本不变。

## 剩余验收

### 桌面旧库结构兼容修复

2026-09-19 后续验收显示鸿蒙可继续准备，桌面报 MIGRATION_RECOVERY_FAILED。读取现有快照并在内存新库恢复，确认实际失败为 purge_cleanup 缺少 local_attempts：早期 schema 22 已执行，后来仅修改该版本建表 SQL，已有库不会再次执行。附件缺失处理已成功记录 7 个文件；这次错误不是网络或 S3 权限问题。

- 桌面新增 schema 23 迁移：字段缺失时补为 NOT NULL DEFAULT 0；已有字段及重试次数原样保留，重复启动不重置。
- 恢复支持本机 schema 22/23 快照；列名集合严格验证并按名称映射，不再要求新旧库物理列顺序相同。仅容许旧 v22 的已知 local_attempts 缺列，并校验其补充值为 0；其他缺列、重复列、未知列继续拒绝。
- 保持源快照摘要、逐字段内容、外键、附件元数据与文件哈希校验。新旧快照不覆盖正式数据库；错误返回安全阶段码，避免全部合并为泛化恢复失败。
- 只读现有失败快照的内存恢复已通过；完整文件恢复演练使用独立临时副本，原快照与正式业务库不作为恢复写入目标。不操作用户云空间、迁移确认或清空。
- 本次只修改桌面功能。应用正常启动时执行本机 schema 升级，应用版本号仍为 1.2.0；鸿蒙保持现状。前文“schema 不变”指上一轮，不覆盖本次明确的 schema 23 修复。
- 本次验证：Rust 407 passed / 36 ignored（只读快照诊断另行执行通过）；现有失败快照在独立临时副本内通过重开、可用附件和清理验证，日志 %TEMP%/eggdone-recovery-diagnosis-fixed.log。双向隔离 S3 证据 %TEMP%/eggdone-sync-core-4058205685724f66b40abee531235188，临时服务已删除。cargo check、fmt、pnpm check（0/0）和前端构建通过。原生界面重新点击“检查并准备”仍待用户确认。

2026-09-19 实际验收发现：两端“检查并准备”显示通用失败。桌面只读诊断确认，本机恢复资料尚未校验完成；计划中的 17 个附件文件有 7 个在原同步空间返回 404，涉及历史删除附件。部分存在其他本机校验一致副本，但并非全部；没有执行迁移、清空或云端写入。鸿蒙端具体失败码尚待新版诊断确认，不能仅凭相同提示认定同一原因。

已修复两端把附件不存在、读取拒绝、完整性校验及恢复验证错误合并为通用文案的问题。界面仅显示允许的错误码和附件 UUID/文件类型，不暴露存储地址、凭据或本机路径。用户已同意下述缺失附件确认策略；实际数据验收仍待进行。

## 已删除附件缺失处理（2026-09-19）

- 两端“检查并准备”允许记录缺失，但仅限附件自身有 deleted_at、本机源文件及私有备份均不存在、旧空间下载明确返回 404。正常附件、403/401、网络错误、损坏文件、路径/符号链接错误不能跳过。
- 已有且校验一致的本机恢复副本优先用于云快照；不会创建空文件冒充附件，不改写正文、附件元数据或删除记录。只恢复可用文件的隔离演练不再被描述为完整附件备份。
- 界面显示缺失数量和 UUID/原文件或预览文件清单，额外确认未默认勾选；与已有升级/离线修改确认同时满足才可继续。后端也强制检查，不能只靠界面禁用按钮。
- 确认摘要包含本机快照、缺失标记和绑定目标。重新准备重新检查文件并清除勾选；内容、目标或清单变化使旧确认失效。
- 本机/云快照保留 missing 标记。有缺失时发布 eggdone.migration-seed.v2，文件行使用 [name,size,sha256,missing]；无缺失仍使用 v1 三列格式。缺失描述保留，文件内容不发布。活动附件被标为 missing 的种子在登记来源前拒绝。
- 同一来源的其他设备可以读取 v2 缺失记录，并在加入前明确确认；没有更新的客户端会拒绝该格式，不会误读或覆盖。参与迁移设备都应更新此次修复。
- 鸿蒙小附件 GET 为有界 S3 错误页保留最多 64 KiB 的最低接收预算，避免错误页大于附件时提前丢失 404/403 状态；成功响应仍严格验证预期大小和 SHA-256，总接收上限不扩大。
- 旧空间、可用恢复资料和缺失清单继续保留；这不是找回丢失文件的功能。恢复相关便签不会凭空恢复缺失附件。未执行用户真实数据迁移、清空或云端写入。

### 修复验证

- Rust 全量 404 passed / 35 ignored；忽略项单独运行，不能计入单元通过数。
- 前端 429 项、Svelte 类型检查 0 errors / 0 warnings、969 项翻译对齐通过。
- 桌面 Playwright 24 个语言/主题/尺寸/缩放组合和 4 组迁移/清空场景通过，包含缺失清单、双确认、重新准备清除确认及 320px 小窗。
- 鸿蒙真实仓库/服务的 host 测试覆盖缺失记录、可用副本恢复、403/断网/损坏拒绝、恢复演练；协议覆盖 68 个中断点及 v1/v2、活动附件缺失拒绝；面板状态和 66 项传输边界通过。host 证据不代表 ArkUI 真机触控。
- 双端隔离 S3 新增无二进制的已删除附件，分别验证桌面发起/鸿蒙加入、鸿蒙发起/桌面加入，以及后续清空与旧副本重连。最终通过证据：%TEMP%/eggdone-sync-core-7052e9052fc94b2cbfe406327e69eff6；临时服务已清理。包含缺失附件双端 seed v2 互读、未授权拒绝、旧摘要拒绝、ready 回复丢失后重试，以及 404 文件清理幂等性。
- 鸿蒙 Debug HAP 构建成功，日志：%TEMP%/eggdone-missing-build-final.log；桌面前端构建见 %TEMP%/eggdone-missing-desktop-build.log。保留既有编译警告，未安装设备、未改变版本号。本次修改未提交。

- [ ] 桌面原生窗口，以及鸿蒙手机/平板的实际触控、字体、键盘和小窗布局。
- [ ] 使用独立测试空间实测两设备迁移、清空、断网重连和后台退出重开；勿用唯一一份正式资料做破坏性测试。
- [ ] 用户实际 S3/MinIO 的条件创建/条件删除权限、失败续作及附件回收。
- [ ] 用户确认上述验收范围后再发布或进入 LC3 今日计划；代码提交不代表真机验收通过，本轮未提前开展 LC3/LC4。

界面入口、后端清空、正式迁移和普通同步授权已经一起接通。剩余是原生/实际存储验收及其发现的问题，不再把通用迁移基础设施列为下一开发阶段。
