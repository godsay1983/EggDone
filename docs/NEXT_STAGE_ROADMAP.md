# EggDone 桌面端下一阶段 Roadmap

实现方案见 [NEXT_STAGE_IMPLEMENTATION_PLAN.md](NEXT_STAGE_IMPLEMENTATION_PLAN.md)。鸿蒙端配套 Roadmap 位于 `D:\Develop\EggDoneHarmony\docs\HARMONY_NEXT_STAGE_ROADMAP.md`。

## 执行规则

- NS1 至 NS5 必须与鸿蒙端同编号阶段共同评审。
- 每阶段先冻结共享语义和 fixtures，再分别实现平台代码。
- 自动检查通过不等于桌面 UI、系统通知或跨端同步人工验收通过。
- 自定义重复不得在只有一端支持协议时发布。

## DNS0：基线和状态收口

- [x] 核对 `main`、版本、最新 handoff 和工作区状态。
- [x] 区分已有 Roadmap 中的代码完成项与人工验收项。
- [x] 更新 README 中当前能力和限制。
- [x] 记录 Windows 签名、自动更新、高 DPI、macOS 和 Linux 剩余门槛。
- [x] 运行现有 `pnpm release:check` 建立基线。

### 2026-09-05 基线记录

- `main` 基线版本为 `1.0.7`，最新 handoff 为 Linux ksni 托盘适配记录。
- `pnpm release:check` 通过：438 个中英文字典键一致，Svelte 检查 0 错误、0 警告，前端 75 项测试和 Rust 110 项测试通过，生产构建成功。
- Rust 仍有一个 `TraySnapshot.locale` 未读取警告，不影响当前基线，后续修改托盘结构时处理。
- 便签、附件和国际化历史 Roadmap 的未勾选项已明确归类为人工验收证据。
- 仍需人工完成 Windows 双语/主题/窄窗口、高 DPI 与多显示器、真实 S3 / MinIO、代码签名和自动更新检查；macOS 与 Linux 继续按发布平台分别回归。

完成条件：文档、版本和实际功能一致，基线失败项有明确归属。

## DNS1：同步状态持久化与诊断中心

### 协议和模型

- [x] 与鸿蒙端冻结本地 `SyncRuntimeSnapshot` 字段和状态枚举。
- [x] 明确该状态不进入 S3、JSON 导出和完整备份。
- [x] 定义 todos、notes、attachments 三个 dirty domain。
- [x] 定义清洗后的稳定错误码和诊断字段白名单。

### Rust 持久化

- [x] 增加 `sync_runtime_state` migration 和单行状态模型。
- [x] Todo、便签、附件写入成功后在数据库层标记对应 domain dirty。
- [x] 同步 command 记录尝试、分域成功、全局成功和失败。
- [x] 启动时把未完成尝试归一化为 interrupted。
- [x] 删除凭据或关闭同步时保留未上传 dirty 状态。

### 前端

- [x] `syncApi.ts` 增加严格类型查询接口。
- [x] `autoSync.ts` 从持久化快照恢复，不再把内存 store 当作唯一事实。
- [x] 主面板同步胶囊显示重启后仍可靠的状态和时间。
- [x] 设置页增加分域结果、待同步数量、重试和复制诊断信息。
- [ ] 中英文和窄面板布局通过。

### 验证

- [x] 新旧数据库 migration 测试通过。
- [ ] 本地修改后强制退出，重启仍显示未同步。
- [x] Todo 成功、附件失败时不显示全局已同步。
- [ ] 离线、凭据错误、ETag 冲突和重试成功通过。
- [x] 诊断文本敏感信息扫描通过。

### 2026-09-05 实施记录

- SQLite schema 16、三域 dirty 触发器、持久化尝试/结果和诊断中心已实现。
- 每域递增版本号阻止同步上传期间的新修改被旧同步误清。
- 自动检查覆盖全新/旧库 migration、分域 dirty、部分失败、并发写入和凭据清洗。
- 尚待人工：强制退出/重启 UI 恢复、真实 S3 / MinIO 的离线/凭据/ETag/重试，以及中英文最窄窗口视觉。

完成条件：用户在任何重启路径后都能准确判断本地修改是否已到达远端。

## DNS2：智能列表

### 共享规则

- [x] 与鸿蒙端冻结 `overdue`、`next7`、`no_date`、`important`、`recently_completed` 语义。
- [x] 建立日期边界和跨时区共用 fixtures。
- [x] 明确与分组、搜索、隐藏已完成和排序的组合规则。

### 实现

- [x] 增加 `SmartViewId` 本地类型和纯函数筛选。
- [x] 在“更多”菜单增加智能列表入口和计数。
- [x] 使用可关闭筛选胶囊表示当前智能列表。
- [x] 禁止在智能列表子集内拖动排序。
- [x] 本机保存上次选择，不进入 S3。
- [x] 增加中英文空状态和无障碍标签。

### 验证

- [x] 日期、时刻、午夜、夏令时和最近完成边界测试通过。
- [ ] 500 条任务切换无明显卡顿。
- [ ] 默认宽度和最窄面板下按钮、计数不截断。

### 2026-09-05 实施记录

- 规则、组合方式及待验收用例见 [SMART_VIEWS_CONTRACT.md](SMART_VIEWS_CONTRACT.md)，两端使用相同的 31 条 fixture 定义。
- 上海、UTC、纽约共 89 个时区案例在 TypeScript、ArkTS 纯函数与鸿蒙 DTO 中结果一致。
- `pnpm release:check` 通过：469 条国际化键、82 项 Vitest、115 项 Rust 测试，类型检查和构建通过。
- Edge 浏览器模拟 IPC、500 条任务：360×560 英文暗色、420×700 中文亮色、360×420 英文亮色的选择、恢复和清除通过；菜单已加宽并按剩余高度滚动，未出现 label/count 重叠或页面错误。
- 大列表关闭逐项飞入/重排动画，并将筛选时钟的 UI 更新限制为分钟/日期边界或实际数据更新；浏览器开发模式切换到 DOM 稳定约 2–4.1 秒（含并行构建时测量），发布模式实机性能仍待验收。
- 浏览器结果不是原生 Tauri 最窄窗口验收；保留上述两项人工门槛。版本和同步 schema 未变，DNS1 未完成的人工项继续保留。

完成条件：高频筛选最多两次操作可达，且不改变 Todo 顺序和同步内容。

## DNS3：通知直接完成

### 实现

- [x] 核对 Windows 通知动作和 macOS/Linux 降级能力。
- [x] 增加稳定的 complete action 与 Todo UUID 参数。
- [x] 抽取 UI、通知共用的完成用例。
- [x] 重复任务走现有下一实例生成逻辑。
- [x] 完成后刷新提醒、托盘角标、列表和 sync dirty。
- [x] 已完成、已删除、不存在和重复动作按幂等规则处理。

### 验证

- [ ] 普通任务通知完成通过。
- [ ] 重复任务只生成一个下一实例。
- [ ] 面板隐藏时动作不会意外显示主窗口。
- [ ] macOS/Linux 不支持按钮时普通通知仍可用。

完成条件：支持的平台可从通知一步完成任务，业务结果与面板内完成一致。

### 2026-09-05 实施记录

- Windows Toast 增加完成动作；正文仍定位任务，完成回调不调用展示面板。macOS/Linux 保持普通通知降级；Windows 退出整个托盘进程后冷启动动作不在当前能力承诺内。
- 事务内共用完成用例及本机动作回执，处理重复点击、已完成、删除、归档、提醒改期和完成后撤销再收到旧动作；重复实例插入失败时完成和回执一起回滚。
- `pnpm release:check` 通过：82 项 Vitest、119 项 Rust 测试，新增4项通知完成回归；构建、类型、国际化和共享 fixture 检查通过。
- 系统通知的实际点击、后台隐藏面板和其他操作系统降级仍需人工验收，未勾选上述设备项。详细步骤见 [REMINDER_COMPLETION_CONTRACT.md](REMINDER_COMPLETION_CONTRACT.md)。

### 用户测试确认

- 用户已确认本轮通知完成任务功能“测试通过”，同意提交代码。
- 此确认作为本轮功能测试结果记录；未提供逐项设备/系统和用例明细，保留上方细分验收清单，避免推断所有平台组合均已覆盖。

## DNS4：快速收集

### 共享草稿

- [x] 与鸿蒙端冻结 `CaptureDraft` 类型、长度和清洗规则。
- [x] 明确草稿不自动保存、不进入同步对象。
- [x] 定义 Todo 与 Note 的预填映射。

### 桌面入口

- [x] 保留现有全局快捷新增 Todo 行为。
- [x] 增加可配置的全局快速新建便签快捷键。
- [x] 增加安全的 `eggdone://capture` 或命令行捕获入口。
- [x] 单实例参数转发到已有进程并复用现有编辑器。
- [x] 剪贴板只在用户明确操作后读取。
- [x] 中英文和最窄面板布局通过。

### 验证

- [x] 中文、英文、URL、空内容和超长文本通过。
- [ ] 应用隐藏、应用已运行和第二次启动参数通过。
- [x] 恶意 scheme、路径和控制字符被拒绝或清洗。
- [ ] 取消不产生空记录，确认后 dirty 和同步正确。

完成条件：从全局快捷键或受信任外部入口可快速进入待确认 Todo/Note 草稿。

### 2026-09-05 实施记录

- 已实现 DNS4 代码阶段，详见 [快速收集契约](QUICK_CAPTURE_CONTRACT.md)。选择等价 `--capture` CLI，未注册 deep-link 协议。
- 快速便签快捷键默认关闭，可在设置启用；旧任务快捷键保持。接收队列仅驻内存，确认后才保存；不会自动读取剪贴板/网页。
- 任务通过单事务保存正文和快捷语法解析结果，失败回滚；便签沿用 NoteStore 与现有编辑器，保存前保护原编辑内容。
- 自动验证：94项 Vitest、124项 Rust、12项跨端收集 fixture；`release:check` 通过。真实组件浏览器检查覆盖8组语言/主题/窗口（含320×430）及取消/确认，不等同于原生窗口验收。
- 上方“中英文和最窄面板”勾选仅代表浏览器确认层检查；全局快捷键冲突、首次/第二实例唤起、原生焦点隐藏和真实S3双向同步仍待人工验证。

### 人工验收确认

- 用户已确认本轮快速收集功能“人工确认通过”，并授权提交代码；DNS4 本轮功能验收通过，可推进下一阶段。
- 确认来源为用户实测反馈，非自动测试推断。用户未附逐项设备、系统及分享来源明细，上方细分清单保留用于后续发布回归，不据此宣称所有跨平台组合均已覆盖。

## DNS5：自定义重复

### DNS5-A：日历与标识基础（2026-09-05）

- [x] 独立 Rust 规则校验与日历引擎：间隔天、间隔周指定星期、间隔月指定日号、结束日期/次数。
- [x] 固定首个计划日期 anchor_date，月末截断不漂移；根据规则推导实例序号，不依赖当前设备时间。
- [x] UUID v5 稳定实例标识与 60 组跨端共用 fixtures，包含非法规则与日期边界。
- [x] 同步 [日历与实例标识契约](RECURRENCE_CALENDAR_CONTRACT.md) 到两仓库。

本轮仅完成基础算法，不改写旧 repeat_rule，不接入数据库、完成操作、S3、备份或 UI。
验证：`pnpm release:check` 通过（94 项前端测试、126 项 Rust 单测、检查及构建）；鸿蒙同组 fixtures 在三个主机 TZ 下执行 180 次通过。
原生时区转换、夏令时 epoch、真实双端并发去重、旧客户端集成验收尚未完成，不因 UUID 一致而提前勾选。
下一步 DNS5-B/HNS5-B：冻结完整规则文档、时区策略、同步冲突和删除语义，再接入两端持久化及事务。

### DNS5-B：规则协议与独立持久化（2026-09-05）

- [x] 冻结 [规则协议 v1](RECURRENCE_RULES_PROTOCOL.md)：严格字段校验、固定时区策略、墓碑优先、进度单调合并、活动关联冲突和键名碰撞检查。
- [x] schema17 新增独立规则表、活动关联唯一索引、版本/ETag 状态；批量合并、dirty 触发器和回滚处于同一事务。
- [x] 相同数据重放不增加版本；过期 ACK 不清除新修改；缺少远端记录不解释为删除。
- [x] 47 组共享协议 fixtures；新库、v16 升级、重复迁移、失败回滚及原有 Todo 文档往返保持规则不变。

验证：`pnpm release:check` 通过；收尾新增旧 Todo 往返测试后，全部 133 项 Rust 单测通过。
两端契约/fixtures 一致，鸿蒙生产 Repository/主机 SQLite 回归通过。未进行真实 S3 或设备联动验收。
边界：只实现规则文档存储；尚未接入用户创建规则、Todo 完成事务、定时 epoch 转换、规则网络请求或备份。
下一步 DNS5-C/HNS5-C：实现时区适配与“当前任务完成/跳过、下一任务插入、规则推进”的统一事务，再接入独立 S3 同步域。

### DNS5-C1：时区与只读推进计划（2026-09-05）

- [x] 已提交 DNS5-B：`e791ceb`，未推送远端。
- [x] 使用 Jiff 与随包 IANA 时区数据库；固定规则时区，空缺向后平移、重复时刻取较早瞬间，未知 ID 拒绝。
- [x] 新增只读推进计划：当前任务证据校验、完成/删除跳过、耗尽、幂等重放、单次生成及时间戳单调保护。
- [x] 两端共享 29 组时区和 22 组推进 fixtures；并发计划下一 UUID 一致，输入对象不被修改。
- [x] DNS5-C2：内部 Todo/规则写入事务、日期物理映射、字段继承及已存在 UUID 的冲突处理（正常入口和 S3 映射仍待 D 阶段）。

实现与边界见 [时区与推进计算](RECURRENCE_TIME_AND_PROGRESS.md)。这一步不写入 Todo，不开放 UI，不接入规则 S3 请求，不修改数据库版本或旧快捷重复行为。
`pnpm release:check` 通过（94 项前端测试、135 项 Rust 测试、国际化、Svelte 检查、Vite 构建和 Cargo 检查）；原生鸿蒙时区、真实 S3 并发、任务写入事务不在本轮自动验证结论内。
下一步 DNS5-C2/HNS5-C2：先固定全天日期和提醒继承映射，再串行协调任务完成/删除、下一项插入和规则推进事务；C1 不代表 NS5-C 整体完成。

### DNS5-C2：内部统一事务（2026-09-05）

- [x] C1 已用中文提交：`d50519c`，未推送。
- [x] 在 IMMEDIATE 事务内读取当前任务证据；完成/跳过、下一项插入、规则推进和提醒回执原子提交。
- [x] 下一项继承有效字段和定时提醒间隔；全天存 due_date，新增实例回执记录计划与投影，schema 不变。
- [x] 重复动作不新增；已有下一项核对系列归属后保留编辑/墓碑；清理掉的已回执实例不复活。
- [x] 8 组事务回归：插入、规则、实例回执和提醒回执故障均回滚 Todo、规则及 dirty。
- [ ] 独立规则对象的真实上传、跨设备并发收敛和备份恢复。
- [ ] 正常完成/删除入口、规则创建与 UI 联动。

完整约定见 [统一事务契约](RECURRENCE_TRANSACTION_CONTRACT.md)。本阶段只开放内部 Rust 接口，不添加 Tauri command，不影响旧任务完成逻辑。
验证：`pnpm release:check` 通过，包含 94 项前端测试、143 项 Rust 测试及构建/类型/国际化检查。原生鸿蒙事务与真实 S3 双端收敛不由这些结果代替。
下一步 DNS5-D/HNS5-D：独立 S3 规则域，接入全天日期映射、下载合并、补算、按序条件上传与正常任务操作编排。之后再开放 UI。

### DNS5-D1：独立规则条件传输（2026-09-05）

- [x] C2 已用中文提交：`54f039c`，未推送。
- [x] 新增独立规则 GET/PUT，复用 rust-s3 签名；严格区分 404、失败与条件冲突。
- [x] 原样强 ETag、下载结果身份校验、3 MiB 有界读取及请求超时；未知写入结果不得 ACK。
- [x] 37 组共用响应 fixtures，6 组本地 HTTP 回归；中文/百分号路径、条件头和读写超时通过。
- [ ] DNS5-D2/HNS5-D2：全天日期映射、补算、Todo 先于规则上传、完整流程有限重试、dirty 和配置切换。
- [ ] 正常任务操作、备份与 UI 接通，以及真实 S3/旧客户端互操作。

详见 [规则传输契约](RECURRENCE_TRANSPORT_CONTRACT.md)。本轮先完成可独立测试的传输层，未将其接入现有同步流程；自定义重复 UI 继续关闭。
验证：`pnpm release:check` 通过，94 项前端与 149 项 Rust 测试通过。未使用真实 S3 凭据，未推送或修改版本号。
下一步 D2 先完成鸿蒙全天日期的物理映射与实例关联，再接入跨领域同步；D1 不代表 NS5-D 整体完成。

### DNS5-D2A：全天日期映射（2026-09-05）

- [x] D1 已用中文提交：`5526bc7`，未推送。
- [x] 与鸿蒙共用 8 组日期往返 fixtures，生产 SQLite 合并/导出覆盖全天与定时切换、清空、完成、删除及重放。
- [x] 桌面端沿用已有 due_date 字段；本步不改变本地 schema17 或同步 JSON v1。
- [x] 冻结两端日期映射、编辑保留和旧数据不猜测修复的边界，见 [全天日期契约](TODO_DATE_SYNC_CONTRACT.md)。
- [ ] D2 后续：首实例关联、规则合并补算、Todo 先于规则上传、完整流程重试及 dirty CAS。
- [ ] 原生 RDB/真实 S3 往返、普通规则操作及备份恢复验收；规则 UI 仍关闭。

本步完成日期映射子阶段，不能将 DNS5-D2 整体标记完成。新的实现保留未提交供下一轮确认。

验证：`pnpm release:check` 通过，包含 150 项 Rust 测试和共享日期往返回归；真实 S3、跨设备人工验证尚未执行。

### DNS5-D2B：规则与任务关联准备（2026-09-05）

- [x] D2A 已用中文提交：`c02f9a5`，未推送。
- [x] 关联预检区分 ready/bind/reconcile/missing/conflict/archived，识别活跃规则争用首实例。
- [x] 单事务补齐首实例关联；有缺失或冲突则整个批次不绑定，不重建任务，不覆盖改期。
- [x] 绑定仅修改 Todo 关联及冲突版本，重放无额外 dirty；失败整体回滚。
- [x] 与鸿蒙共用 17 组关联 fixtures，生产 SQLite 测试覆盖补写、缺失后到、失败重试和时钟/关联冲突。
- [ ] D2C：生产同步编排、确定性补算、Todo 先于规则上传、完整流程重试与分领域 ACK。
- [ ] 普通规则操作、备份恢复、实际 S3 及原生平台验收；UI 保持关闭。

详见 [关联准备契约](RECURRENCE_LINK_PREPARATION.md)。can_prepare 不是上传确认，不允许跳过 Todo 上传或使用过期结果。本步未启用新网络请求，已提交为 `92b0ab0`，未推送。

验证：`pnpm release:check` 通过，154 项 Rust、94 项前端测试全通过，Svelte 检查无错误和警告。真实跨设备同步仍待后续接入验收。

人工反馈：用户确认当前桌面端与鸿蒙端同步正常。此记录仅覆盖已开放的同步功能，不代表未接入的自定义规则同步已验收。

### DNS5-D2C1：一致上传快照准备（2026-09-05）

- [x] D2B 已用中文提交：桌面 `92b0ab0`、鸿蒙 `5f6b484`，未推送。
- [x] 同一 IMMEDIATE 事务完成关联准备、确定性补算、Todo/规则校验和序列化。
- [x] 同时捕获 Todo 和规则版本；返回独立字符串，不清 dirty、不报告上传成功。
- [x] 缺失、冲突、后置归档阻塞以及写入/校验/提交失败全部回滚，不复活已清除实例。
- [x] 每次最多推进 128 次，超限整体回滚；稳定状态重放不重复生成或制造 dirty。
- [x] 9 项新增 Rust 测试覆盖全天/定时、提醒偏移、结束条件、多步补算、改期保留、回执、真实延迟约束提交失败及上限边界。
- [ ] D2C2：生产网络编排、配置切换防护、Todo 先于规则上传、完整流程有限重试、分领域 CAS ACK 和提醒刷新。
- [ ] 自定义规则普通操作、备份恢复、原生 RDB/真实 S3 验收；UI 保持关闭。

详见两端一致的 [快照契约](RECURRENCE_SNAPSHOT_CONTRACT.md)。这一步仅实现内部准备，不改变当前已开放同步流程；已提交为 `aae73d0`，未推送。未改版本号、schema 或实况窗。

验证：`pnpm release:check` 通过，163 项 Rust、94 项前端测试通过，Svelte 检查 0 错误、0 警告，生产构建成功；保留既有 `TraySnapshot.locale` 未读取警告。鸿蒙对应 19 项宿主 SQLite 快照场景通过，不能等同真实双端网络验收。

### DNS5-D2C2A：双文档同步编排内核（2026-09-05）

- [x] D2C1 已用中文提交：桌面 `aae73d0`、鸿蒙 `0bbcea0`，未推送。
- [x] 独立 RecurrenceSyncPort / run 统一顺序：整对下载、合并准备、Todo PUT/ACK、规则 PUT/ACK。
- [x] 任一条件上传冲突都重走完整流程，最多两轮；持续阻塞/冲突停止，其他错误不做盲目重试。
- [x] 在 I/O 边界检查目标有效性；分领域确认捕获版本，收尾复查版本并返回 local_changes_pending。
- [x] 共享 20 组流程 fixtures；SQLite 真实快照、远端合并和 ACK 测试覆盖部分成功、较新远端数据与上传期间本地编辑。
- [x] D2C2B：生产配置世代、S3/数据库适配、主入口与规则轮询、错误映射和刷新实现及自动回归完成；真实 S3/设备验收仍见后续门槛。
- [ ] 规则普通操作、备份恢复、实际 S3 双端验收；UI 继续关闭。

详见两端一致的 [同步编排契约](RECURRENCE_SYNC_FLOW_CONTRACT.md)。本轮完成的是可执行编排内核，不是生产同步已接入；测试传输端为替身，配置校验回调不等于已经实现真实配置并发保护。已提交为 `6e2e27e`，未推送。

验证：`pnpm release:check` 通过，165 项 Rust、94 项前端测试通过，Svelte 0 错误/0 警告，生产构建成功；保留既有 `TraySnapshot.locale` 警告。主同步入口、schema、版本号、签名、实况窗均未改动。

### DNS5-D2C2B1：真实会话适配与配置世代（2026-09-06）

- [x] D2C2A 已用中文提交：桌面 `6e2e27e`、鸿蒙 `e4e10df`，未推送。
- [x] 配置保存/凭据删除先持久化新世代，并递增所有领域版本；失败保持 pending，旧会话不能复用。
- [x] 新规则会话调用生产 S3/规则传输与 SQLite 合并、原子快照、短事务 ACK；网络请求不占数据库锁。
- [x] 整对有限重试、迟到回包拒绝、部分成功与上传期间编辑保留已接到真实适配器。
- [x] 新增 2 项配置事务测试、3 项生产适配器回环 HTTP 测试；`pnpm release:check` 通过：170 项 Rust、94 项前端测试，Svelte 0 错误/0 警告，生产构建成功。
- [x] D2C2B2：主入口与规则发现、统一排他、错误映射、尝试/成功摘要及提醒/UI 刷新（自动验证完成，见下节验收边界）。
- [ ] 规则普通操作、备份恢复和真实跨端 S3 验收；规则 UI 仍关闭。

详见两端一致的 [生产会话契约](RECURRENCE_SYNC_SESSION_CONTRACT.md)。B1 当时尚未替换主流程，现已由下节 B2 接入。B1 已提交 `cce1bfc`，未推送；回环 HTTP 不是用户真实 S3 验收。

### DNS5-D2C2B2：主同步接入与规则发现（2026-09-06）

- [x] B1 已用中文本地提交：桌面 `cce1bfc`、鸿蒙 `b1a989f`，未推送。
- [x] `sync_now` 捕获一次配置世代、持有整会话排他；Todo 阶段首次发现远端规则，再按 Todo → 规则条件上传，后续便签/附件沿用同一目标。
- [x] 9 项跨端发现用例：空/404 不写空规则；403 仅在无本地规则、无远端候选时保守兼容旧 Key 策略；401/5xx 不能静默当作无规则。
- [x] 各领域按实际上传快照 ACK；未上传二进制及并发规则编辑保持待同步。配置切换后旧回包不更新本地确认或成功/失败摘要。
- [x] 部分失败也刷新任务、便签和托盘；规则网络错误有限重试，配置/凭据错误与冲突不额外网络重试，复用中英文错误提示。
- [x] `pnpm release:check`：172 项 Rust、101 项前端，Svelte 0 错误/0 警告，生产构建通过；保留既有 TraySnapshot.locale 非阻断 Rust 警告。
- [x] D2C2B3：独立规则 ETag 轮询、配置切换后的旧探测隔离及自动回归（见下节）。
- [ ] 普通规则操作、备份恢复、原生平台与真实双端 S3 验收；这些门槛完成前不开放规则 UI。

B2 已本地提交 `5c0473a`，未推送；未改版本、schema 或签名。真实 S3、系统提醒与窗口人工测试仍待执行，不承诺跨 Bucket 的历史附件搬迁。

### DNS5-D2C2B3：规则轮询与探测隔离（2026-09-06）

- [x] B2 已用中文本地提交：桌面 `5c0473a`、鸿蒙 `a9a000a`，未推送。
- [x] 前台探测追加独立规则 HEAD；200 强 ETag、404 missing、403 denied 分开处理，401/5xx/缺失 ETag 不推进观察基线。
- [x] 规则单独变化触发同步，失败保留待处理观察；成功只采信规则 PUT 回执，不用收尾 HEAD 吞掉其他设备刚写入的规则版本。
- [x] 配置保存/禁用、手动同步及页面隐藏使旧探测失效；后端在四次 HEAD 边界检查捕获世代，迟到错误不污染新目标状态。
- [x] `pnpm release:check`：174 项 Rust、111 项前端，Svelte 0 错误/0 警告，生产构建通过；新增 8 项共享探测用例及配置并发、轮询行为回归。
- [x] E1：普通完成/取消完成入口、幂等和提交后刷新已接入，见下节。
- [x] E2：普通单项/选中项删除、跳过、恢复和系列停止事务已接入，见下节；不代表全部 UI 入口已验收。
- [ ] 真实 S3、Windows 窗口/前台恢复及鸿蒙设备验收；自定义规则 UI 继续关闭。

B3 已本地提交为 `90b2d55`，未推送。沿用约 60 秒前台轮询，不承诺后台实时同步；未改版本、schema、签名或实况窗，NS5-D 整体仍有普通操作和备份等后续任务。

### DNS5-E1：普通任务完成与提交后刷新（2026-09-06）

- [x] 页面/批量、专注窗口 UUID 完成和系统通知共用原子完成事务，接入自定义规则推进。
- [x] 历史项取消完成不回退规则，重复完成不生成第二份下一项；保留通知回执与未来逻辑时钟。
- [x] 列表按 UUID 合并下一项，过滤墓碑/归档，批量部分失败保留已完成项并触发同步。
- [x] 已有下一项保留编辑，规则/实例/提醒回执写入失败整笔回滚；原快捷重复保持独立算法。
- [x] `pnpm release:check` 通过：184 项 Rust、115 项前端测试、Svelte 检查 0 错误/0 警告和生产构建通过；仅保留原有 TraySnapshot.locale 未使用警告。
- [x] 鸿蒙 19 项普通完成/副作用宿主场景、29 项规则事务、9 项旧提醒完成、39 项同步会话回归通过。
- [ ] 原生设备、真实 S3 双端/离线冲突验收；E2 删除/跳过、CRUD 和规则备份恢复。

详细边界见 [普通操作契约](RECURRENCE_TASK_ACTION_CONTRACT.md)。E1 已本地提交 `425ae08`，未推送；规则 UI 继续关闭，未改版本、schema、签名或实况窗。

### DNS5-E2：删除本次、系列停止与撤销（2026-09-06）

- [x] 删除当前项通过既有 skip 事务推进一次，删除历史项及重复动作不重复生成。
- [x] 系列停止写规则墓碑，系列删除将停止与已有活动任务墓碑原子提交，失败整笔回滚。
- [x] 撤销只恢复任务且时钟单调，不恢复规则/撤回下一项；无法识别的自定义形状系列拒绝系列删除。
- [x] 删除结果返回下一项，列表 UUID 去重、批量顺序结果和部分失败刷新覆盖。
- [x] `pnpm release:check` 通过：196 项 Rust（含 12 项删除事务）、119 项前端（含新增 4 项列表回归）、Svelte 检查 0 错误/0 警告及生产构建；保留原有 TraySnapshot.locale 警告。
- [x] 鸿蒙 22 项删除/恢复宿主场景、19 项完成、29 项规则事务、9 项旧提醒完成、39 项同步会话回归通过。
- [ ] E3：规则 CRUD、备份恢复及清空已完成/归档等入口的开放前复核；自定义规则 UI、设备和真实 S3 验收仍未完成。

E2 已本地提交 `25884a6`，未推送。两端语义和降级边界见 [普通操作契约](RECURRENCE_TASK_ACTION_CONTRACT.md)，无版本/schema/签名/实况窗变更。

### DNS5-E3A：规则创建、替换与停止内核（2026-09-06）

- [x] 普通任务附加初始规则、当前任务与旧规则快照并发校验、首日期/固定时区校验和原子绑定。
- [x] 修改周期使用新 UUID，旧规则墓碑与当前项转为新系列首项同事务；保留历史任务。
- [x] 本地编辑回执防止重试重复创建或倒退进度；停止接口保持幂等，只停规则不删除任务。
- [x] 10 项新 Rust 编辑事务回归；完整发布检查通过（206 Rust / 119 前端，Svelte 0 错误/0 警告及生产构建）。
- [x] 鸿蒙 32 项编辑宿主回归、19 项快照、22 项删除、19 项完成通过；debug HAP、98 LocalTest、国际化与静态检查无阻断错误。
- [x] E3B：规则备份恢复和编辑/实例回执策略，见下节。
- [x] E3C：规则摘要、创建编辑 UI、操作后提醒刷新与其他入口复核，见 E3C 记录。
- [ ] 原生设备、真实 S3 双向及离线冲突验收；候选版开放编辑入口，不能据此标记 NS5 全部完成。

接口与明确未实现项见 [规则编辑契约](RECURRENCE_EDITOR_CONTRACT.md)。E3A 随本阶段功能提交入库，提交与推送状态以最新 handoff 和 Git 为准；无版本/schema/签名/实况窗变更。

### DNS5-E3B：规则备份与恢复（2026-09-06）

- [x] JSON 与完整备份 data.json 升为 v2，包含规则和可移植实例回执；继续支持旧 v1，旧客户端明确拒绝 v2。
- [x] 同一事务合并所有元数据；规则进度不倒退、停止不复活、已清理实例不重建；恢复不生成任务。
- [x] 恢复与同步互斥，恢复成功清除规则 ACK/ETag 并标脏；编辑回执失效，不迁移凭据和通知消费回执。
- [x] 双端导入预览显示规则数，补充中英文失败提示，保持现有入口。
- [x] 共享全天/定时备份样例、桌面 11 项专项测试、鸿蒙 22 项宿主场景通过；包含真实生产解析器和数据库写入，不仅是静态字符串检查。
- [x] 最终回归：桌面 release:check 通过（217 Rust、119 前端，Svelte 0 错误/0 警告）；鸿蒙 debug HAP、98 LocalTest（0 Failure/0 Error）、545 英文伪本地化及 MCP 0 错误通过。既有编辑 32、快照 19、删除 22、完成 19、同步会话 39 项宿主回归通过。
- [ ] 原生文件选择器/RDB、手机/平板亮暗中英文、真实 S3 及完整附件跨端恢复集中人工验收。

详见 [规则备份恢复契约](RECURRENCE_BACKUP_CONTRACT.md)。E3B 已提交；E3C 候选版入口现已接通，见下节。不改变版本、数据库 schema、签名或实况窗。

### DNS5-E3C：自定义重复编辑界面（2026-09-06）

- [x] 独立规则编辑器，保留四个快捷选项；支持日/周/月间隔、星期多选、月内日/末日和日期/次数结束。
- [x] 共享首项计算和表单校验，保存前预览日期；全天与固定时区定时，明确旧提醒清除和修改周期重新计数。
- [x] 列表摘要、创建/替换/停止、只读历史限制及中英文错误；失败重试保留请求，不覆盖过期任务或规则。
- [x] 普通日期入口保留自定义系列，阻止旧快捷选项覆盖；未保存任务修改不被新编辑入口静默丢弃；清空/归档回归。
- [x] 桌面固定头尾与滚动弹层；鸿蒙共享手机/平板受控宽度独立编辑层，胶囊按钮、可换行操作。
- [x] 桌面 release:check：220 Rust / 147 前端、Svelte 0 错误/0 警告、生产构建。17 个真实 Svelte 浏览器场景（mock IPC），含窄屏/中英文/亮暗、重试及未保存日期保护；最后入口保护修改后另跑 check 通过。
- [x] 鸿蒙新增 31 个表单/UI 宿主场景、98 LocalTest；MCP 0 错误；debug HAP、595 条英文伪本地化检查通过，覆盖安装到 Mate 80 Pro Max 模拟器并启动成功，未卸载。编辑 32、备份 22、快照 19、完成 19、删除 22、同步会话 39 项回归。
- [ ] 原生 IPC/RDB、手机/平板输入法与大字体、提醒/卡片、真实 S3 和离线双端冲突集中验收；自动化不能替代。

入口、限制、布局和测试步骤见 [编辑界面契约](RECURRENCE_EDITOR_UI_CONTRACT.md)。此前各日期小节的“UI 关闭”和未完成条目是当时阶段记录；本节与下方汇总表示当前开发状态，设备/发布门槛仍保留。

### 跨端协议门槛

- [x] 与鸿蒙端冻结 `recurrence-rules.json` v1 schema。
- [x] 固定 Object Key、ETag、墓碑和冲突决胜规则。
- [x] 固定 occurrence key 和跨语言 UUID v5 fixtures。
- [x] 固定月末、时区、结束日期、结束次数和漏过实例策略。
- [ ] 验证旧客户端同步 Todo 时不会触碰规则对象。

### 桌面数据层

- [x] 增加 recurrence rule migration、Repository 和 Rust 类型。
- [x] 实现规则 CRUD、当前实例关联和删除墓碑（E1/E2/E3）。
- [x] 实现确定性下一实例生成和并发去重（C2/E1）。
- [x] 同步增加独立规则对象、dirty、ETag 和有限冲突重试（D2）。
- [x] 导入导出和 `.eggdone-backup` 纳入规则元数据（E3B，设备互导验收另列）。

### 桌面 UI

- [x] 保留四个现有快捷重复选项。
- [x] 增加独立“自定义重复”弹层。
- [x] 支持间隔、指定星期、月内日期和结束条件。
- [x] 列表和编辑器显示可读摘要。
- [x] 英文长摘要可截断并通过 tooltip 查看完整内容。

### 验证

- [ ] Rust 与 ArkTS 共用 fixtures 全部通过。
- [ ] 两端离线同时完成不产生重复实例。
- [ ] 月末、闰年、夏令时和跨时区通过。
- [ ] 旧备份、旧同步对象和旧客户端兼容测试通过。

完成条件：两端可安全创建和处理相同自定义重复，旧客户端不会损坏 Todo 主对象。

## DNS6：后续能力决策

- [x] 基于已有任务/便签/附件反馈，优先任务与便签关联；暂缓检查清单，不伪造强制子项完成需求。
- [x] 专注历史首版仅本地保存，和活动计时恢复分开，不新增同步对象。
- [x] 完成独立 link 模型、原子操作、删除/恢复、同步/备份及两端入口设计。
- [x] 完成无服务端加密同步的恢复、轮换、忘密和威胁模型；正式加密实现仍需安全评审。
- [x] 选定能力已有独立实现方案和 Roadmap；其他候选在独立方案前禁止顺带编码。

2026-09-06：NS6 设计决策完成，见 [决策与威胁模型](NEXT_CAPABILITIES_DECISIONS.md)、[关联方案](TASK_NOTE_LINK_IMPLEMENTATION_PLAN.md) 与 [关联 Roadmap](TASK_NOTE_LINK_ROADMAP.md)。关联 L1–L5 是 NS7 之后的候选阶段，不是已实现能力。当前继续 NS7 发布回归。

完成条件：至少选定一项下一阶段能力，并明确不做项和数据边界。

## DNS7：回归与发布

- [x] `pnpm release:check` 通过（2026-09-06，220 Rust / 147 前端，Svelte 0 错误/0 警告）。
- [ ] Windows 中英文、亮暗主题、窄面板和高 DPI 回归通过。
- [ ] Windows 通知动作、托盘、快捷键和自动更新验证通过。
- [ ] Linux ksni、凭据库、AppImage/deb 和通知降级通过。
- [ ] macOS 菜单栏、窗口定位、快捷键和通知降级通过。
- [ ] 与鸿蒙端完成真实 S3 / MinIO 双向同步和冲突测试。
- [x] 更新 README、发布说明草稿、候选版本 1.0.8 和 handoff；未推送/发布。

NS7 自动回归与开发资料已完成，仍非正式发布验收完成。17 个真实 Svelte 浏览器场景（mock IPC）通过，但不替代上述系统门槛。桌面上传超时回归隔离前置下载，鸿蒙 UTC 原生问题已修复；全部结果和剩余 R1-R8 集中验收见 [候选版记录](NS7_REGRESSION_AND_RELEASE.md)。历史人工确认不能直接覆盖新自定义规则的完整验收。

完成条件：自动检查、平台人工验收和双端协议验收均有记录，可生成正式发布包。

## 推荐提交边界

1. `docs(next): define desktop reliability and workflow roadmap`
2. `feat(sync): persist desktop sync runtime state`
3. `feat(views): add desktop smart lists`
4. `feat(reminders): complete tasks from desktop notifications`
5. `feat(capture): add desktop quick capture entry points`
6. `feat(recurrence): add desktop custom recurrence foundation`
7. `feat(sync): sync desktop recurrence rules`
8. `test(release): close desktop next-stage regression`
