# E6 桌面原生重启验证

## 当前结论

2026-09-13：新增原生重启测试运行器和结果检查器，**原生用例尚未跑通，不作为 D1 或 E6 验收通过证据**。没有修改生产逻辑、版本、同步配置或普通客户端数据。

先前两次运行分别停在找不到主页面、等待主页面超时；第三次改为等待 CDP `/json/list` 中的主页面，最终也在启动阶段结束。第三次目录为 `%TEMP%/eggdone-preferences-native-677f364b26d9407d9137abf383e0e57b`：没有生成恢复快照或阶段报告，stdout/stderr 为空。它只证明测试未进入偏好写入，不能据此断定产品持久化故障，也不能确定主页面缺失的根因。

本轮检查确认上述运行已结束，9228 无监听者，没有遗留隔离应用；普通客户端 PID 52336、可执行路径和启动时间保持不变。本轮新的隔离应用启动命令被执行策略拒绝，未改用其他入口绕过。以下脚本改动完成后只运行了语法和检查器测试，没有再次执行原生流程。

## 测试设计

- `scripts/run-preferences-native.ps1` 只接受 `src-tauri/target/search-native` 内的构建，拒绝已有同路径进程和占用的 9228 端口。启动后立即记录 PID、启动时间、二进制 SHA256；每轮启动采样保存目标页面 URL、错误和尝试次数，避免超时后只留下空日志。
- `scripts/test-preferences-native.mjs` 在写入前核对 `com.eggdone.searchtest`、同步关闭且无凭据。快照只保存在本轮临时目录；不使用普通客户端数据库，不创建任务或便签。
- 写入阶段从生产设置界面选择英文、日历默认视图、45 分钟专注、15 分钟休息、舒适窗口；切换深色、日历当前视图，固定“无日期”和“未来 7 天”。原生记录确认完成后，在隔离配置中放置冲突的旧 localStorage 值。
- 重启阶段要求新进程恢复原生值、实际窗口尺寸和缩放、界面选项、固定入口，并检查独立专注窗口的语言、主题和未启动计时。不会开始专注或注册测试提醒。
- 最后恢复隔离应用原偏好及三项旧 localStorage 值，校验后结束本轮拥有的进程；恢复会增加修订号，原来缺失的通用偏好键可能变为语义相同的 null，不是数据库字节级还原。
- `scripts/check-preferences-native-report.mjs` 同时要求写入、重读、恢复报告通过、两次不同进程身份、清理完成、无页面错误。启动超时、缺阶段报告、恢复失败均不能输出整体通过。

**这是强制结束隔离进程后的持久化测试，不是托盘“退出”测试。** 分组、显示已完成、系统快捷键冲突、自启动、设备重启、多显示器、Linux 和真实提醒仍需各自验证。即使脚本以后通过，也不能勾选整个 D1 或完整 E6。

## 本轮验证

- `node --test scripts/test-preferences-native-report.mjs`：20/20，包含 1 项完整合成报告和 19 项拒绝路径。它证明检查器不接受缺失/失败/错配证据，不证明应用重启成功。
- `node --check scripts/test-preferences-native.mjs`、PowerShell Parser：通过。
- `pnpm check`：0 错误、0 警告；`pnpm build`、`cargo fmt -- --check`、`cargo check`：通过。Rust 保留已有 `TraySnapshot.locale` 未使用警告。
- 原生流程：未通过，保留启动诊断门槛。新增目标页面采样和恢复分支尚待真实运行验证。

## 后续复跑

需要允许启动本地应用的环境，以及 Rust/MSVC、Tauri、Playwright。先按 [统一搜索原生构建步骤](CONTENT_SEARCH_NATIVE_RUNTIME.md#复现步骤)生成当前源码的 `com.eggdone.searchtest` 独立构建；不能直接替换普通客户端可执行文件。设置本机 `PLAYWRIGHT_PATH` 后在桌面仓库运行：

```powershell
pwsh -NoProfile -File scripts/run-preferences-native.ps1
```

失败后先检查输出目录的 `result.json` 和 `write-startup.json`，再查看当前进程、端口及对应阶段日志。不得因为观察超时就启动第二份进程；运行器结束时会清理自己创建且身份仍匹配的进程。恢复失败时保留 `recovery.json`，先核对隔离标识和失败原因，再在同一安全环境恢复，不覆盖旧快照重跑。

结果通过后还需查看主窗口、设置、专注窗口截图，并单独完成 D1 的托盘退出和未覆盖偏好；截图文件存在或报告通过不能代替视觉验收。
